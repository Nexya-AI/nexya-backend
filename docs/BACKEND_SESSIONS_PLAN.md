# 📋 Plan de sessions — Backend NEXYA

> **Source de vérité unique pour piloter le développement backend session par session.**
> **Auteur :** Loth Ivan Ngassa Yimga
> **Dernière mise à jour :** 2026-04-22
> **Format de session :** 10 heures de travail intensif effectif
>
> **Plan opérationnel frontend symétrique :** [`../../nexya_front_end/docs/FRONTEND_SESSIONS_PLAN.md`](../../nexya_front_end/docs/FRONTEND_SESSIONS_PLAN.md) — 42 sessions × 8-10 h ≈ 378 h pour finir le frontend, table dépendances back-end synchronisée pour pilotage couplé.

---

## 🎯 Comment utiliser ce fichier

1. **Au début de chaque journée de travail**, dire à Claude : « aujourd'hui on fait la Phase X » ou « on attaque le bloc Y session Z ».
2. Claude lit cette section, charge le contexte des fichiers concernés et démarre.
3. **À la fin de chaque session terminée**, marquer la case `[x]` et noter la date dans la colonne « Fait le ».
4. **Le pourcentage global** se met à jour automatiquement en additionnant les sessions cochées sur 57.

---

## 📊 Vue d'ensemble — Avancement global

> **Backend code pur restant : 57 sessions de 10 h ≈ 570 heures ≈ 2.5 mois à 10 h/jour 6 j/sem**
> **+ 233 heures d'apprentissage transversal en parallèle**

| Statut | Sessions | % du restant |
|---|---|---|
| ✅ Livré sur ce plan (A1, A3, B1, B2, B3) | 5/57 | 8.8 % |
| 🔧 En cours | 0/57 | 0 % |
| ❌ À faire | 52/57 | 91.2 % |

**État backend global du projet : ~47 % couvert (selon ROADMAP §0, +1 pt depuis B3).**
Une fois ce plan terminé, le backend sera à **~95 %** (les 5 % restants = ajustements post-pentest et polish post-bêta).

---

## 🏆 Ce qui est DÉJÀ livré (rappel)

> Pour ne pas perdre la perspective. Ces blocs sont **terminés** et n'apparaissent pas dans la suite du plan.

- ✅ Infrastructure Core (FastAPI, Postgres, Redis, MinIO, Docker dev, structlog, traces, healthz/ready)
- ✅ Auth de base (10 endpoints : register, login, refresh, logout, /me, profile, password, RGPD anonymisation, device-token × 2)
- ✅ Couche IA Tier 1 (7 briques : Provider ABC, Gemini réel + 3 stubs, Router 11 experts, Moderation, Budget Redis, Retry+CircuitBreaker, StreamHandler SSE, Observabilité tokens)
- ✅ Chat persisté complet (14 endpoints : 6 CRUD + stream + stop + reports + trash + restore + permanent + worker auto-titre)
- ✅ Tests : 63/63 verts (auth hardening + chat service + chat router + chat stream persisté + abuse reports)
- ✅ Validation end-to-end manuelle réussie (2026-04-21)

---

# 🗺️ Plan de sessions par phase

> Légende : ❌ à faire | 🔧 en cours | ✅ fait

---

## **BLOC A — Phase 1 résiduel (Auth durci)**

**Sessions : 3** | **Heures : 30** | **% du restant : 5.3 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| A1 | Reset password + email transactionnel | `POST /auth/forgot-password` (token signé 15 min), `POST /auth/reset-password` (consomme token + invalide refresh tokens), intégration SendGrid/Brevo, templates Jinja2, tests | ✅ | 2026-04-22 |
| A2 | OAuth Google + Apple | `GET/POST /auth/google/start` + `/callback`, `GET/POST /auth/apple/start` + `/callback` (Sign In with Apple), config redirect URIs par env, tests OAuth flow | ❌ | — |
| A3 | Captcha + anti-abus + sanitizer + quotas device | `POST /auth/register` exige hCaptcha server-side, limite >5 inscriptions IP/jour (`rate_limit_register_daily_ip`), **quotas par device fingerprint** (header `X-Device-Id` + table `device_quotas` UPSERT atomique 5/jour/device), table `auth_events` (11 event_types + FK `ON DELETE SET NULL` RGPD-safe + fail-safe insert), `core/security/sanitizer.py` (NFC + null bytes + zero-width/bidi), `core/security/captcha/` (hCaptcha + Mock factory), 29 tests hardening complémentaires (121/121 verts, 0 régression) | ✅ | 2026-04-22 |

**Livrable bloc A :** toutes routes `/auth/*` vertes, OAuth démo fonctionnelle, captcha bloque les bots, suite tests auth 100 %.

---

## **BLOC B — Phase 2 résiduel (Couche IA)**

**Sessions : 3** | **Heures : 30** | **% du restant : 5.3 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| B1 | Câblage SDK réels OpenAI + Anthropic + Qwen | **Mock-first factory** : `MockChatProvider` usurpe l'identité des 4 providers (name, default_model, supported_models, max_context_tokens) → fallback `experts.py` résout sans warning `model_not_in_supported_set` même si la clé est vide. `OpenAIChatProvider` réel (`openai>=1.55` + `stream_options={include_usage:True}`, cas spécial reasoning `o1`/`o1-mini` : drop `temperature` + `system` mergé dans le 1ᵉʳ user, `max_completion_tokens`). `AnthropicChatProvider` réel (`anthropic>=0.42` + `client.messages.stream()` context manager, `system` en kwarg séparé, `max_tokens=4096` par défaut, events `content_block_delta`/`message_delta`/`message_stop` + `get_final_message()` pour usage finale, `_merge_claude_usage` max-cumulatif). `QwenChatProvider` réel (réutilise `openai.AsyncOpenAI` avec `base_url=https://dashscope-intl.aliyuncs.com/compatible-mode/v1`, même mapping d'erreurs). `_map_sdk_exception()` par provider : Auth→`ProviderAuthError`, Rate→`ProviderRateLimitError`+retry-after, BadRequest+content_filter→`ProviderContentFilteredError`, 5xx/timeout/connection→`ProviderUnavailableError` (retryable). Lazy client singleton + `_reset_client_for_tests()`. `asyncio.CancelledError` propagé. `build_default_router()` sélectionne réel vs mock par clé. 33 tests (7 MockChatProvider + 11 OpenAI + 6 Anthropic + 3 Qwen + 3 router factory + 3 live gated par `skipif`). **151/151 tests verts + 3 skipped** (aucune régression sur les 118 tests pré-B1) | ✅ | 2026-04-22 |
| B2 | Cache Redis prompt + garde-fous métiers + estimation `tiktoken` | `app/ai/cache.py` (`PromptCache` Redis SHA-256 canonique sur `(model, messages, system_prompt, temperature, max_tokens, expert_id)`, TTL 24 h, skip safety-critical medicine+legal, skip multi-turn user, fail-open, rejet put sur erreur/troncature/texte vide). `app/ai/moderation_rules.py` : refus prescription médicale nominative (5 regex FR — « prescris-moi X mg », « combien de mg … prendre », « ordonnance pour mon/ma… ») + rédaction d'acte juridique nominatif (2 regex — « rédige un contrat/bail/testament »), disclaimer médecin/avocat dans le message, whitelist par expert vide au lancement (même medicine/legal refusent), kill-switch `moderation_rules_enabled`. `app/ai/token_estimator.py` : `tiktoken` réel pour OpenAI (`o200k_base`) + Qwen (`cl100k_base`), heuristique `chars/3.0 × 1.15 + overhead` pour Gemini/Anthropic, fallback heuristique si tiktoken absent/crash, budget complétion par défaut 1024. Intégration `/chat/stream` : pipeline 1.budget → 2.modération OpenAI → 3.règles métier → 4.résolution expert → 5.estimate tokens → 6.cap 402 `LLM_QUOTA_EXCEEDED` (défaut 30 000 tokens/requête) → 7.lookup cache si legacy stateless → 8a.replay cache HIT / 8b.stream MISS + put fin-de-flux. Header `X-Cache: HIT\|MISS\|BYPASS`. 81 tests adversariaux (26 cache + 15 token_estimator + 17 moderation_rules paramétrés + 12 intégration router + cas limites) — **232/232 verts + 3 skipped, 0 régression** | ✅ | 2026-04-22 |
| B3 | CostTracker DB + SessionStore + QueryEngine consolidé + OpenRouter | **Brique 1 — OpenRouter provider réel** (`app/ai/providers/openrouter_provider.py` via `openai.AsyncOpenAI` + `base_url=https://openrouter.ai/api/v1`, headers optionnels `HTTP-Referer` + `X-Title` lus depuis settings, 5 modèles curés : `anthropic/claude-3.5-sonnet` (défaut), `meta-llama/llama-3.1-70b-instruct`, `mistralai/mistral-large`, `deepseek/deepseek-chat`, `qwen/qwen-2.5-72b-instruct`, `max_context_tokens=128_000`, même hiérarchie d'erreurs que OpenAI SDK via `_map_sdk_exception`). **Brique 2 — Tables `ai_calls` + `usage_daily` + `CostTracker` DB** : migration Alembic 004, table `ai_calls` (17 colonnes + UNIQUE `session_id` + index `(user_id, created_at DESC)` + FK `ON DELETE SET NULL`), table `usage_daily` (PK composite `(user_id NULLABLE, date_utc)`), `CostTracker.record_ai_call` **fail-safe** (jamais raise), `record_ai_call_background` fire-and-forget via `asyncio.create_task`, pipeline : INSERT ai_calls + UPSERT `usage_daily` UNIQUEMENT pour outcome ∈ {completed, cancelled}, IntegrityError UNIQUE session_id → rollback + log `cost_tracker.duplicate_session`, `_to_decimal` conversion float→Decimal via `str()` (évite IEEE 754), `_jsonify` dict→JSON string pour JSONB. **Brique 3 — SessionStore Redis + flush arq cron** : `app/ai/engine/session_store.py` (SET TTL 24 h + SCAN non-bloquant + fail-safe sur toutes les ops), cron `flush_ai_sessions` toutes les 10 min (`workers/ai_tasks.py`) : INSERT `ai_calls` ON CONFLICT (session_id) DO NOTHING RETURNING → delete Redis après succès (idempotent grâce à la contrainte UNIQUE). **Brique 4 — QueryEngine consolidé** (extraction pragmatique) : `app/ai/engine/query_engine.py` expose `DONE_REASON_TO_STATUS` (mapping aligné CHECK SQL `stop→completed`/`cancelled→cancelled`/`error→failed`), dataclass `StreamOutcome` accumulateur (`done_reason`/`error_code`/`content_parts` + `final_status()` + `final_content()`), fonction pure `observe_sse_event()` parsing `event:` / `data:` / `:` comments, classe `QueryEngine.run()` orchestre `StreamHandler.stream()` + observation outcome, `app/features/chat/router.py` + `tests/test_chat_stream_persisted.py` migrent vers ces imports publics (alias `_StreamOutcome`/`_observe_sse_event`/`_DONE_REASON_TO_STATUS` conservés). 76 tests B3/B4 (15 query_engine + 15 cost_tracker + 21 session_store + 25 openrouter), **308/308 verts + 3 skipped** (232 précédents + 76 B3/B4), 0 régression | ✅ | 2026-04-22 |

**Livrable bloc B :** chaîne fallback prouvée en test live (kill API key OpenAI → bascule Anthropic), modération bloque 10 prompts toxiques, coûts persistés en DB par user.

---

## **BLOC C — Phase 5 (History + Projects + Library)**

**Sessions : 3** | **Heures : 30** | **% du restant : 5.3 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| C1 | History avec recherche FTS française | Migration 005 (pg_trgm + generated `search_vector` tsvector FR + GIN) sur `messages.content` + trigram GIN sur `conversations.title`, extension `GET /chat/conversations?q=` (ILIKE sur titre + EXISTS FTS `plainto_tsquery('french', …)` sur messages reliés par OR, sort keyset préservé), 8 nouveaux tests (5 router + 3 service SQL shape), datasource Flutter `listConversations(q:)` | ✅ | 2026-04-23 |
| C2 | Projects CRUD complet | Migration 006 : tables `projects` (id, user_id, name ≤100, icon_index [0..24], color_index [0..17], instructions ≤4000, timestamps, deleted_at) + `project_files` (id, project_id, name ≤255, file_type enum 8 valeurs, storage_key/size_bytes/mime_type nullables pour upload E3, uploaded_at, deleted_at) + FK `conversations.project_id ON DELETE SET NULL` + 5 index partiels (unique case-insensitive `(user_id, LOWER(name))` actifs, trigram GIN sur `name` réutilisant pg_trgm C1). Exceptions `ProjectQuotaExceededException` / `ProjectFilesQuotaExceededException` (402) et `ProjectNameConflictException` (409). `ProjectService` avec 9 méthodes (create avec quota pré-flight Free=3/Pro=50 + IntegrityError→409, list_for_user keyset + q trigram, get 404 IDOR, update PATCH partial + `clear_instructions`, soft_delete détache conversations via UPDATE SET project_id=NULL, list_conversations délégué à `ConversationService.list_for_user(project_id=...)` réutilisant FTS C1, add_file quota pré-flight Free=5/Pro=100, list_files keyset, remove_file idempotent). Router 9 endpoints `/projects` alignés convention NEXYA (PATCH pas PUT, `NexyaResponse[T]`, 404 IDOR, pagination cursor ≤50). **38 nouveaux tests** (19 service couvrant SQL shape via literal_binds + 19 router TestClient). Compteurs `file_count`/`conversation_count`/`last_activity_at` calculés SQL via subqueries scalaires (évite N+1). | ✅ | 2026-04-24 |
| C3 | Library CRUD | Migration 007 : table `library_items` (18 colonnes métadonnées alignées `MediaModel` Flutter + `content_sha256` dédup + `metadata_json` jsonb extensible + FK `source_conversation_id`/`source_message_id` ON DELETE SET NULL) + 6 index partiels (user+active, **UNIQUE partial `(user_id, storage_key)` pour dédup idempotente via SHA-256**, user+type, user+conv, trigram GIN sur title, GIN sur tags). Nouveau wrapper `app/core/storage/object_store.py` : ABC `ObjectStore` + `S3ObjectStore` (aioboto3 context-manager strict, idempotent `delete`, bucket auto-create, presigned URL HMAC local) + `MockObjectStore` RAM (mock-first auto si `s3_access_key` vide). `LibraryService` avec `create_from_bytes` (upload MinIO → INSERT ON CONFLICT DO NOTHING RETURNING : re-upload du même contenu retourne l'existant sans erreur + sans duplicate storage), `create_from_base64` (wrapper validation), `list_for_user` (keyset + filtres combinables type/source/conversation_id/q trigram), `get`, `soft_delete` sans delete MinIO synchrone (cleanup différé phase 12). Router 4 endpoints `/library` avec presigned URLs (TTL 1 h) exposées dans chaque response sans fuite `storage_key`/`sha256`. 2 nouvelles exceptions (`LibraryQuotaExceededException` 402, `ObjectStoreUnavailableException` 503). 7 nouveaux settings (`s3_use_ssl`, `s3_region_name`, `s3_presigned_ttl_seconds`, `s3_max_upload_bytes`, `storage_auto_create_bucket`, `storage_mock_enabled`, `library_max_free/pro`). **Auto-save `/image/generate`** — chaque image Imagen retournée est persistée automatiquement avec `source='generated'` + provider + model + prompt, réponse enrichie avec `library_ids`, fail-safe (upload ko → log + 200 avec `library_ids=[]`, l'IA reste payée mais l'user n'est pas pénalisé). **54 nouveaux tests** (10 ObjectStore + 22 service SQL shape via literal_binds + dédup ON CONFLICT + quota + cap taille + validators mime/type + 12 router TestClient + 7 autosave avec fail-safe partiel). Alembic env étendu (LibraryItem ajouté au metadata autogenerate). | ✅ | 2026-04-24 |

**Livrable bloc C :** créer projet « École », assigner 3 conversations, chercher « intégrale » → résultats, sauver image dans library, la retrouver.

---

## **BLOC D — Phase 6 (Mémoire pgvector + RAG + attribution sources)**

**Sessions : 5** | **Heures : 50** | **% du restant : 8.8 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| D1 | Infrastructure embeddings + table memories | Migration 009 (`CREATE EXTENSION IF NOT EXISTS vector` idempotent + table `memories` 14 colonnes dont `embedding vector(1536)` + `content_sha256` CHAR(64) + `embedding_model`/`embedding_dim` traçabilité forensique + `source` ∈ {manual,extracted,imported,system} + FK `source_conversation_id`/`source_message_id` ON DELETE SET NULL + `importance 0-10` + `metadata_json` JSONB + 5 index partiels dont UNIQUE `(user_id, content_sha256) WHERE deleted_at IS NULL` pour dédup idempotente + **HNSW `vector_cosine_ops` m=16 ef_construction=64**). Module `app/ai/embeddings/` complet (ABC `EmbeddingsProvider` avec contrat batch `embed(texts) -> EmbeddingsResponse` dans l'ordre + hiérarchie erreurs typées miroirs ChatProvider B1 : `EmbeddingsAuthError` / `RateLimitError` avec retry_after / `UnavailableError` / `InvalidRequestError`) + `OpenAIEmbeddingsProvider` (`text-embedding-3-small` 1536 dim par défaut, supporte aussi `3-large` 3072 dim et `ada-002` legacy, client lazy `AsyncOpenAI(max_retries=0)` même discipline B1, `_map_sdk_exception` miroir direct OpenAIChatProvider) + `MockEmbeddingsProvider` SHA-256 → 1536 bytes → centrés [-1,1] → L2-normalisé norm=1.0±1e-6 (déterministe, dim identique au vrai modèle, compatible colonne DB, aucune sémantique — tests sémantiques via `@pytest.mark.skipif(not OPENAI_API_KEY)` Phase N2) + factory `get_embeddings_provider()` singleton lazy mock-first automatique. `MemoryStore` service avec `add` (normalize content = trim + collapse whitespace interne → SHA-256, quota pré-flight Free=100/Pro=10k → 402 `MEMORY_QUOTA_EXCEEDED`, BudgetTracker embeddings pré-flight → 429 `RATE_LIMIT_EXCEEDED`, `embed([normalized])`, `pg_insert.on_conflict_do_nothing(index_elements=['user_id','content_sha256'], index_where=deleted_at.is_(None)).returning(Memory)` + SELECT fallback si conflit), `search` (validation query non-vide + embed query + SQL raw `SELECT ... 1 - (embedding <=> CAST(:q_vec AS vector)) AS similarity ... WHERE ... ORDER BY embedding <=> CAST(:q_vec AS vector) LIMIT :k` retourne `list[MemorySearchResult(memory, similarity)]` triée par similarité décroissante, k clampé [1,50], filtre optionnel `source` et `min_similarity`), `get_for_user` 404 IDOR-safe, `soft_delete` pose `deleted_at`, `delete_for_user` **RGPD hard DELETE physique + commit + retour count** (à brancher sur `DELETE /user/account` Phase J), `count_for_user` utilitaire. 2 nouvelles exceptions (`MemoryQuotaExceededException` 402 data={current, max, plan}, `EmbeddingsUnavailableException` 503 data={provider, reason}). Extension `BudgetTracker.check_and_consume_embeddings(user_id, cost=1)` avec défaut `DEFAULT_USER_EMBEDDINGS_PER_DAY=10_000` (surchargeable via kwarg constructeur). 8 nouveaux settings (`openai_embedding_model`, `embeddings_mock_enabled`, `embeddings_dim=1536`, `embeddings_content_max_chars=2000`, `embeddings_batch_max_size=100`, `memory_max_free=100`, `memory_max_pro=10_000`, `budget_embeddings_per_day=10_000`). Dep `pgvector>=0.3,<1` (pure Python + bindings C optionnels pour pack compact des floats, ajoute `pgvector.sqlalchemy.Vector(N)` type ORM). **34 nouveaux tests** (9 mock déterminisme+dim+norm+batching+usage+rejet liste vide / 5 runtime factory mock auto+forcé+OpenAI impl+singleton+reset / 16 MemoryStore helpers+add happy+dédup ON CONFLICT+rejets validation/quota/budget/embeddings_fail+search query vide+empty rows+404 IDOR+soft_delete+RGPD hard delete count+count_active / 4 BudgetTracker incr normal+cap raise+cost N+isolation user). **511/511 verts + 3 skipped**, 0 régression, 2 runs back-to-back exit 0. **Socle interne uniquement — aucun endpoint HTTP exposé** (D2 extraction, D3 injection system prompt, D4 RAG documents, D5 endpoints `/memory/*` publics). | ✅ | 2026-04-24 |
| D2 | Extraction faits durables post-conversation | Migration 010 (`conversations.memory_extracted_at TIMESTAMPTZ NULL` sentinelle one-shot + index partiel `ix_conversations_memory_pending (updated_at) WHERE memory_extracted_at IS NULL AND deleted_at IS NULL AND message_count >= 6` pour cron fallback Phase 12). Nouveau `workers/memory_tasks.py` (~450 lignes) avec 2 publics + helpers : **`enqueue_memory_extraction(conversation_id)`** pool arq lazy miroir chat_tasks, fail-silent (try/except + log), **`extract_durable_facts(ctx, conversation_id)`** worker arq avec pipeline strict 10 étapes court-circuitantes (1. parse UUID + log start / 2. AsyncSessionLocal fraîche / 3. `db.get(Conversation)` → skip si missing/deleted/already_extracted / 4. SELECT messages completed `limit EXTRACTION_MAX_CONTEXT_MESSAGES=20` → skip si `< EXTRACTION_MIN_MESSAGES=6` SANS poser sentinelle pour re-enqueue futur / 5. SELECT User owner → skip si user purgé RGPD / 6. build `list[AiChatMessage]` + appel `_call_llm_for_facts` Gemini Flash `temperature=0.2, max_tokens=500` → skip 'llm_failed' SANS sentinelle / 7. `_parse_facts_json` 3 passes (direct `json.loads` / regex `{...}` balanced pour markdown-wrapped / fallback `[]`) + post-filter (str only, strip, truncate 200 chars, dédup interne set lowercase, cap `EXTRACTION_MAX_FACTS=3`) / 8. boucle faits : validation `EXTRACTION_FACT_MIN_CHARS=10` + `_is_sensitive(fact)` filtre keyword FR/EN `SENSITIVE_KEYWORDS frozenset` (santé 10+, finance 5+, religion/politique 10+, orientation 8+, syndicat 3+) → skip sensitive ; sinon `MemoryStore.add(source='extracted', source_conversation_id=conv_uuid, importance=3, metadata_json={extraction_model, extraction_provider, extraction_timestamp, conversation_message_count})` avec try/except (MemoryQuota/RateLimit/EmbeddingsUnavailable/Validation) → continue boucle / 9. sentinelle `UPDATE conversations SET memory_extracted_at=NOW() WHERE memory_extracted_at IS NULL` + commit (posée MÊME si 0 fait extrait — évite boucle infinie sur conv stérile) / 10. return stats `{skipped: False, facts_extracted: N, facts_skipped_sensitive: M, facts_skipped_other: K}`). **Prompt système** FR strict demandant JSON `{"facts": [...]}` max 3 faits, 10-200 chars, 3ème personne "L'utilisateur...", exclusion données sensibles RGPD, détection langue auto. **Hook router chat** dans `_finalize_in_fresh_session` après title enqueue : `should_enqueue_memory_extraction = status=='completed' AND memory_extracted_at IS NULL AND message_count >= EXTRACTION_MIN_MESSAGES` → `await enqueue_memory_extraction(conversation_id)` hors du `with` DB, fail-safe. Worker enregistré dans `WorkerSettings.functions` de `workers/worker.py`. **46 nouveaux tests** (13 parser : JSON direct valide + empty facts + markdown wrapped + préfixé + broken → [] + empty raw + facts non-liste + items non-str filtrés + dédup case-insensitive + truncate > 200 + strip whitespace-only + cap EXTRACTION_MAX_FACTS / 10 sensitivity : 14 paramétrés vrais positifs FR+EN multi-catégories + 6 vrais négatifs neutres + case-insensitive + faux positif substring documenté + empty/whitespace / 10 worker : skip conv missing/deleted/already_extracted/not_enough_messages/user_missing, happy path 3 faits avec `source='extracted'` + sentinelle posée, LLM failure sentinelle NON posée, JSON cassé sentinelle POSÉE 0 faits, MemoryStore quota raise continue boucle, filtre sensibilité skip le fait, enqueue Redis down log+return sans raise). Alignement pattern éprouvé B5 auto-title. Discipline sécurité RGPD Article 9 : filtre conservateur recall > precision (mieux rater 1 fait légitime que stocker 1 donnée sensible sans consentement). | ✅ | 2026-04-24 |
| D3 | Injection top-5 memories dans system prompt | Nouveau `app/features/memory/context_builder.py` (~150 lignes) avec `async build_memory_context(user, db, *, query, k=None, min_similarity=None, provider=None) -> str | None` : short-circuits (`memory_injection_enabled=False` → None, query vide → None) + `MemoryStore.search` D1 + format bloc markdown structuré via `_format_memories_block(results)` (en-tête avec **instructions d'usage LLM** « utilise si pertinent, ne les mentionne pas explicitement sauf si demandé », liste `- fait (pertinence: 0.XX)` triée par similarité décroissante, footer `[/Contexte]`, troncature post-cap avec marqueur `[... contexte tronqué]` si dépasse `memory_injection_max_chars=2000`) + **fail-safe absolue** (except Exception → log warning + return None, chat ne doit JAMAIS être bloqué par la mémoire). Extension `StreamContext.memory_context: str | None = None` dans `app/ai/streaming.py`. Concat définitive dans `_stream_link` (streaming.py:454) : `system_prompt_final = memory_context + "\n\n" + config.system_prompt` si memory, sinon `config.system_prompt`. **Single Source of Truth** : la concat se fait UNIQUEMENT dans `_stream_link`, le router calcule une version locale `system_prompt_for_check` pour le token estimator + cache key mais propage `memory_context` brut via `StreamContext`. Hook dans `chat_stream` (router) après construction des `ai_messages`, AVANT le token estimator (ligne ~537) pour que le cap 30k tokens B2 prenne en compte le bloc mémoire (anti-contournement). Cache key B2 inclut aussi `system_prompt_for_check` → miss attendu entre users différents (memories user-specific). Propagation du `memory_context` dans les 2 `StreamContext` (legacy stateless + persisté). 4 nouveaux settings (`memory_injection_enabled=True` kill-switch, `memory_injection_k=5` top-K, `memory_injection_min_similarity=0.7` seuil plancher anti-pollution contexte, `memory_injection_max_chars=2000` cap total protection prompt tokens). **23 nouveaux tests** (11 context_builder + 7 concat streaming + 5 router intégration). **580/580 verts + 3 skipped**, 0 régression, 2 runs back-to-back exit 0. Aucune migration DB (pure logique applicative), aucune dep nouvelle. **Boucle Bloc D fermée** : D2 extrait → D1 indexe → D3 injecte automatiquement. Socle interne — aucun endpoint HTTP public (reste D5). | ✅ | 2026-04-24 |
| D4 | RAG documents : chunking + indexation pgvector | Migration 011 (`uploaded_files.chunks_indexed_at` sentinelle + `CREATE TABLE document_chunks` BigInteger PK autoincrement + offsets `start_char_offset`/`end_char_offset` + `page_number INT NULL` + UNIQUE `(file_id, chunk_index)` + 5 CHECK constraints dont `end_char_offset >= start_char_offset` + `ALTER TABLE ... ADD COLUMN embedding vector(1536) NOT NULL` + HNSW `vector_cosine_ops` m=16 ef_construction=64 + 2 btree user/file + index partiel `ix_uploaded_files_chunks_pending` pour cron fallback). Modèle `DocumentChunk` ORM avec `pgvector.sqlalchemy.Vector(1536)`. Helper `app/features/files/text_cleaner.py` (fonction pure pipeline 6 passes : NFC + déhyphénation `(\w)-\n(\w) → \1\2` + strip lignes `3/10` et `Page N` multiline + collapse whitespace horizontal + collapse ≥3 sauts de ligne + strip, **préserve les marqueurs `[[PAGE:N]]`** car regex spécifiques aux chiffres isolés). Extension `text_extractor._extract_pdf(..., inject_page_markers: bool = False)` qui insère `[[PAGE:N]]\n` avant chaque page quand demandé (rétro-compat totale : default False préserve comportement E3). Module `app/features/files/chunker.py` avec dataclass `Chunk(index, content, token_count, start_char_offset, end_char_offset, page_number)` + `chunk_text(text, target_tokens=500, overlap_tokens=50)` : `_extract_page_ranges` retire les marqueurs et reconstruit `[(start, end, page), ...]` dans le cleaned, boucle soft-break priorité `\n\n > \n > . space > space` recherchée dans 2ᵉ moitié de la fenêtre pour garantir taille minimale, token_count via `tiktoken.cl100k_base` module-load, `_resolve_page` via milieu du chunk (robuste pour overlap chevauchant 2 pages), garde-fou anti-boucle infinie (`next_cursor = max(cursor+1, end-overlap)`). Worker `workers/chunk_tasks.py::index_document_chunks` pipeline 14 étapes court-circuitantes : (1-3) load UploadedFile + short-circuits `missing`/`deleted`/`already_indexed`/`mime_not_supported` / (4) sémaphore Redis `chunk:sem:{user_id}` INCR + check vs `max_concurrent_chunking_per_user` + DECR rollback + TTL configurable, saturation → `raise arq.Retry(defer=30)` / (5) `ObjectStore.download_bytes(storage_key)` → `FileNotFoundError` → skip `storage_missing` + pose sentinelle / (6) `asyncio.to_thread(extract_text, ..., inject_page_markers=(mime=='pdf'))` / (7) `asyncio.to_thread(clean_extracted_text)` / (8) garde-fou `len(cleaned) < documents_pre_clean_min_chars` → skip `text_too_short` + sentinelle / (9) `asyncio.to_thread(chunk_text)` / (10) cap truncation `documents_chunks_per_file_max` + flag `truncated=True` / (11) embed par batches de `documents_embed_batch_size=100` avec **re-check cancel mid-way** via `_is_file_cancelled` (session éphémère ré-interroge `deleted_at`, stop propre sans INSERT si soft-deleted pendant embed) / (12) re-check final avant INSERT / (13) bulk `db.add_all(rows)` + `UPDATE uploaded_files SET chunks_indexed_at=NOW()` / (14) log forensic `documents.chunk.completed` dict complet (`n_chunks`, `total_tokens`, `embeddings_cost_usd`, `duration_ms`, `truncated`, `pages`, `embedding_model`). Release sémaphore obligatoire en `try/finally`. `max_tries=3` sur la fonction. Helper `_mark_indexed` pose la sentinelle sans INSERT (évite boucle infinie sur docs stériles). Extension ABC `ObjectStore.download_bytes(key) -> bytes` + impl Mock (dict lookup + `FileNotFoundError` si absent) + impl S3 (aioboto3 `get_object` + `body.read()` dans context manager, discrimination message → `FileNotFoundError` vs `ObjectStoreUnavailableException`). Hook dans `FileUploadService.upload` : **quota pré-flight** `_check_documents_quota` AVANT lecture bytes (COUNT actifs scopé mimes éligibles vs `documents_max_{free,pro}` → 402 `DOCUMENTS_QUOTA_EXCEEDED` avec `data={current,max,plan}`) + enqueue fail-silent `enqueue_chunking(row.id)` après succès pipeline pour mimes `_CHUNKING_ELIGIBLE_MIMES` (PDF/DOCX/TXT/MD seulement, images/audio/vidéo exclus). Nouvelle exception `DocumentsQuotaExceededException` 402 alignée pattern Project/Library/Memory. **4 settings pricing `# TODO(Ivan): provisoire`** (`documents_max_free=3`, `documents_max_pro=50`, `documents_chunks_per_file_max=500`, `max_concurrent_chunking_per_user=2`) + 5 settings technique (chunk target=500/overlap=50, embed batch=100, pre-clean min=50 chars, sémaphore TTL=600 s). **42 nouveaux tests** (7 text_cleaner + 12 chunker + 3 object_store_download + 5 files_service_enqueue + 15 chunk_worker) — 42/42 verts. TODO(D5) explicite dans `chunker.py` pour défense prompt injection côté lecture RAG (framing `<<<DOCUMENT EXTRACT>>>` + instruction système). **Rule G worst-case** : 1 Pro à cap 50 docs × 500 chunks × 500 tokens = 12.5M tokens × $0.02/1M = $0.25/user ; 950k users worst-case one-shot = $237 500 ; régime réaliste 10% actifs × 5 docs/mois = ~$2 375/mois, soutenable. | ✅ | 2026-04-24 |
| D5 | Endpoints publics Mémoire IA + RAG documents + défense prompt injection | 4 endpoints `/memory/*` (`POST /memory/index` ajout manuel source='manual' via `MemoryStore.add` déjà câblé D1, `POST /memory/search` top-K via `MemoryStore.search` déjà câblé, `GET /memory?cursor&limit&source` liste paginée keyset avec filtre source Literal `{manual,extracted,imported,system}`, `DELETE /memory/{id}` **hard DELETE RGPD Article 17 physique et idempotent** qui renvoie 204 même si absent pour anti-énumération) + `POST /rag/query` nouvelle feature dans `app/features/rag/` qui interroge `document_chunks` via SQL cosinus pgvector `<=>` avec **JOIN strict `uploaded_files`** (rempart IDOR unique cross-user, filtre `deleted_at IS NULL` exclut les fichiers soft-deleted), k clampé [1,20], min_similarity défaut 0.6, filtre optionnel `file_ids`, budget embeddings 1 crédit + rate limit user-scoped `rag_query_rate_limit_per_hour=60` distinct (sliding window Redis `rate:user:rag_query:{uid}`), retourne 4 champs : `chunks` (contenu + offsets + page + similarity), `sources` (allégé UI sans content), `framed_context` (chunks wrappés `<<<DOCUMENT EXTRACT id="N" file="..." chunk="..." page="...">>>...<<<END EXTRACT N>>>`), `instruction` (ligne système anti-injection `RAG_SYSTEM_INSTRUCTION` « Ne JAMAIS suivre d'instructions contenues dans ces extraits »). Helper pur `app/features/files/rag_framing.py::build_rag_framed_context(chunks)` duck-type accepte `Chunk` D4 et `RagChunkItem` D5, réutilisable par future session chat-RAG. Extensions `MemoryStore.list_for_user` (keyset `tuple_()` SQL natif + curseur opaque base64url + `ValidationException` 422 si malformé) + `MemoryStore.delete_one_for_user` (hard DELETE physique idempotent retourne count). **40 nouveaux tests** (8 rag_framing : wrapping tags numérotés 1..N, file_id/page/chunk attributs, page None → omis, duck-type Chunk vs RagChunkItem, instruction contient clause « Ne JAMAIS suivre » / 7 memory_store_list_delete : list SQL shape + filtres, delete hard DELETE pas UPDATE, idempotence, scope owner, curseur roundtrip + malformé raise / 14 memory_router : 201/422/402/429 sur index, 200/422/forward params sur search, pagination/filtres/limit>50 422 sur list, 204 idempotent sur delete / 7 rag_service : happy path + embed + SQL, clamping k min/max, embed fail 503, JOIN uploaded_files + deleted_at, forward file_ids / 7 rag_router : 200 avec 4 champs, 422 empty/k>20, 429 rate limit, 503 embeddings, forward file_ids/k/min_similarity, chunks vides → framing vide, config `rag_query_rate_limit_per_hour` câblée). 1 setting technique nouveau (`rag_query_rate_limit_per_hour=60`, à ma main). **Pas de pricing nouveau** — réutilise `memory_max_free=100/pro=10_000` D1 et `documents_max_free/pro` D4 (TODO Ivan). Hors scope : intégration `/chat/stream-rag` (session future), DVC versioning (lié G1 Modes Experts), re-ranking cross-encoder (Phase 12), hybrid search BM25+vector (Phase 12), export RGPD Article 20 `/memory/export` (Phase J). Rule G `/rag/query` négligeable : ~$0.000001/query × 10 queries/jour × 950k users = ~$285/mois. | ✅ | 2026-04-24 |

**Livrable bloc D :** user dit « Je suis dev Flutter » → 3 jours plus tard « écris-moi du code » → IA propose Dart sans re-précision. Upload PDF → question dessus → réponse avec sources cliquables.

---

## **BLOC E — Phase 7 (Voice + Vision + Files + Watermarking C2PA)**

**Sessions : 4** | **Heures : 40** | **% du restant : 7.0 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| E1 | Voice STT + TTS **Pro only** — asymétrie cost-smart | Stratégie Free = STT/TTS natif Flutter (`speech_to_text` + `flutter_tts`, offline, zéro backend, $0 coût). Pro = endpoints gated `Depends(require_pro)` → 403 `PLAN_REQUIRED` Free → Whisper API qualité premium pour Pro. Migration 012 `voice_transcriptions` (UUID PK + user CASCADE + FK uploaded_files SET NULL + SHA + `model`/`provider`/`cost_usd` tracés par row pour benchmark portabilité + CHECK + UNIQUE partial SHA dédup + 2 btree index). Nouveau module `app/ai/voice/` avec ABC `VoiceProvider` + `TranscriptionResult`/`TTSResult` dataclasses + hiérarchie erreurs typées (VoiceAuth/RateLimit/Unavailable/InvalidRequest), `OpenAIVoiceProvider` (`AsyncOpenAI.audio.transcriptions.create` + `audio.speech.create`, `max_retries=0` RetryPolicy ext, `_map_sdk_exception` miroir B1), `MockVoiceProvider` déterministe (texte synth basé SHA + MP3 silencieux), factory `get_voice_provider()` singleton mock-first auto si `OPENAI_API_KEY` vide OU `voice_mock_enabled=True`. 2 endpoints `/voice/*` gated Pro : `POST /voice/transcribe` pipeline 13 étapes strictement court-circuitantes (MIME whitelist 7 formats → cap 20 MB streaming read + SHA → magic-bytes check réutilisé E3 → estimation durée via heuristique MP3 16k bytes/s → refus 413 `AUDIO_TOO_LONG` si > 10 min → dédup SHA cross-upload retourne existing sans API → quota pro pré-flight 402 `VOICE_QUOTA_EXCEEDED` → `BudgetTracker.check_and_consume_voice_minutes` → rate limit user-scoped 30/h → provider.transcribe avec mapping erreurs → `refund_voice_minutes` si estimation > durée réelle → INSERT VoiceTranscription + log forensic), `POST /voice/speak` pipeline 8 étapes (validation Pydantic 1-4096 chars → quota + budget TTS chars → rate limit 60/h → provider.synthesize → si `save_to_library=True` fail-safe `LibraryService.create_from_bytes(type='audio', source='generated', provider, model, prompt[:200], voice, chars)` + presigned URL → sinon `StreamingResponse(audio/mpeg)` direct). 4 nouvelles exceptions (`VoiceQuotaExceededException` 402 data={current,max,plan}, `TTSQuotaExceededException` 402, `AudioTooLongException` 413 data={duration,max}, `VoiceUnavailableException` 503 data={provider,reason}). Extensions `BudgetTracker` : `user_voice_minutes_per_day` + `user_tts_chars_per_day` + `check_and_consume_voice_minutes` + `check_and_consume_tts_chars` + `refund_voice_minutes` fail-safe. **2 settings pricing `# TODO(Ivan): provisoire`** (`voice_minutes_pro_per_day=120`, `voice_tts_chars_pro_per_day=50_000`) + 10 settings techniques (cap upload 20 MB, cap durée 600 s, rate limits, 8 MIMEs whitelist, 3 modèles/voix par défaut, flag mock force). Aucune valeur pricing Free (Free = natif Flutter). 46 nouveaux tests (5 provider_mock : déterminisme + rejet fmt / 7 provider_openai : mapping rate-limit retry-after + auth + connection + cost formula / 4 runtime : mock-first + singleton + reset / 5 budget : voice_minutes + tts_chars + refund + isolation / 12 service : pipeline rejets MIME/cap/durée/quota, dédup sans API, refund excès, provider down mappé 503, synthesize fail-safe library sans id / 13 router : **2 tests dédiés 403 PLAN_REQUIRED Free**, Pro happy 201/200, streaming audio direct, 402/413/415/429/503 propagation, 422 Pydantic). **Rule G Pro-only recalculée** : 1 Pro cap max = $1.47/jour ; 95k Pros actifs régime réaliste × 5 min/jour × $0.006 = ~$2.85k/mois STT + ~$210/mois TTS = **~$3k/mois totalement auto-financé par ~$570k/mois de revenu Pro** (hypothèse 6 €/mois × 95k Pros). Hors scope : real-time voice WebSocket `gpt-4o-realtime` (Phase 8), voix clonée ElevenLabs (Phase 10), diarization pyannote (Phase 12), endpoint traduction Whisper, chunking audio > 10 min (Phase 12 si demande), endpoint `/voice/list` voix dynamiques (micro-session quand Flutter demande). Portabilité stratégique garantie : le jour où Ivan switche Whisper → faster-whisper self-hosted GPU Hetzner (~$100/mois fixe vs ~$3k/mois variable) ou Deepgram Nova-2 (-28%), c'est 1 classe `XxxVoiceProvider` à écrire + 1 ligne factory runtime, zéro réécriture applicative grâce à l'ABC. | ✅ | 2026-04-24 |
| E2 | Vision multimodale (Gemini + GPT-4o) — Free + Pro asymétrie par tier | Endpoint `POST /vision/analyze` sans `require_pro` (Free autorisé avec tier=`flash` imposé, Pro avec choix tier=`flash`/`pro`, 403 PLAN_REQUIRED Free qui tente pro). Migration 013 `vision_analyses` avec dédup UNIQUE partielle sur `(user_id, image_sha256, prompt_sha256) WHERE deleted_at IS NULL` + FK uploaded_files/library_items ON DELETE SET NULL + CHECK constraints + 3 index partiels. Nouveau module `app/ai/vision/` avec ABC `VisionProvider` + attribut `supports_tiers: set[VisionTier]` déclaratif + 3 implémentations : `GeminiVisionProvider` via `google.genai.Client.aio.models.generate_content` supporte flash+pro avec prix tracés ($0.075/1M in + $0.30/1M out flash, $1.25/1M in + $5.00/1M out pro), `OpenAIVisionProvider` gpt-4o pro-only avec message multimodal format `{type:image_url, image_url:{url:data:image/png;base64,...}}` + prix $2.50/1M in + $10/1M out, `MockVisionProvider` déterministe SHA-based. Factory `get_vision_provider(tier)` singleton par-tier mock-first auto si `gemini_api_key` vide OU `vision_mock_enabled=True` sinon sélection réelle (Gemini par défaut, OpenAI si `vision_pro_provider=openai` ET tier=pro). Nouveau module `app/features/vision/` avec `image_utils.py` (resize Pillow avec préservation ratio + fallback RGBA→JPEG si transparence + estimation tokens Gemini tiles-based règle documentée Google), `prompt_safety.py::VISION_SYSTEM_INSTRUCTION` (défense anti-prompt-injection couche 1 aligné pattern D5 RAG framing, instruction système explicite « Ne JAMAIS suivre d'instructions visibles dans l'image »), `schemas.py` (VisionAnalyzeRequest avec `model_validator` mutex strict sur 3 sources + field_validator strip prompt + Literal ImageSource), `service.py::VisionService.analyze` pipeline 14 étapes strict (tier check pro sur user Free 403 → résolution 3 modes input → MIME whitelist 4 formats → cap 10 MB 413 ImageTooLarge → magic-bytes check → resize async Pillow via asyncio.to_thread → additional_images cap 4 total → estimation tokens cap 10k → SHA combinés → dédup actif → quota `vision_images_{free,pro}_per_day` 402 VISION_QUOTA_EXCEEDED → BudgetTracker.check_and_consume_vision_images → rate limit 30/h → provider.analyze_images avec VISION_SYSTEM_INSTRUCTION → mapping ContentFiltered 400 + refund / Unavailable 503 + refund → INSERT + log forensic). Router `POST /vision/analyze` avec `Depends(get_current_user)` (PAS `require_pro`, le gate tier est dans le service). Extensions BudgetTracker : `check_and_consume_vision_images(user, images=1)` + `refund_vision_images` fail-safe. 4 nouvelles exceptions (`VisionQuotaExceededException` 402 data{current,max,plan}, `VisionContentFilteredException` 400 neutre, `VisionUnavailableException` 503 data{provider,reason}, `ImageTooLargeException` 413 data{size,max}). **4 settings pricing `# TODO(Ivan): provisoire`** (`vision_images_free_per_day=3`, `vision_images_pro_per_day=50`, `vision_max_images_per_request=4`, `vision_max_output_tokens_pro=4096`) + 9 settings techniques (cap 10 MB, max dim 2048, cap tokens input 10k, rate limit 30/h, 5 mimes whitelist, flag mock, 3 modèles/voices par défaut, provider pro gemini|openai). Nouvelle dépendance `Pillow>=10,<12` (resize + encoding). **63 nouveaux tests** (6 mock + 8 gemini SDK-mocked + 6 openai SDK-mocked + 5 runtime factory + 4 budget + 6 image_utils Pillow + 16 service pipeline + 12 router avec propagation codes erreur). **Rule G recalculé** : Free 3 img/jour × Gemini Flash $0.00018/req × 950k users × 5% actifs régime réaliste = **~$255/mois**, Pro 50 img/jour × Gemini Pro ~$0.0025/req × 10% Pro actifs = **~$3.6k/mois**, total **~$3.9k/mois soutenable** (Pro auto-financé par abo, Free marginal). Portabilité stratégique garantie ABC. Hors scope : `/chat/stream` avec images inline (session future chat-vision), SSE streaming tokens, vidéo analyse (Phase 8), C2PA détection (E4), Tesseract fallback (Phase 12), historique endpoints publics (GET/DELETE `/vision/analyses/*`), batch > 4 images. | ✅ | 2026-04-24 |
| E3 | Files upload + S3 wrapper | **`core/storage/s3.py` déjà livré en C3** (wrapper `ObjectStore` ABC + `S3ObjectStore` aioboto3 + `MockObjectStore` + factory mock-first). **E3 livre le pipeline d'upload** : migration 008 (table `uploaded_files` 18 cols + 3 index partiels dont UNIQUE `(user, SHA256)` pour dédup cross-feature), nouveau module `app/core/storage/mime_detector.py` pure Python (12 formats magic-bytes + discrimination OOXML via `zipfile`+`xml.etree` stdlib — zéro dep libmagic), `app/core/storage/text_extractor.py` (PDF via `pypdf` + DOCX zipfile+xml stdlib + plain UTF-8/latin-1, cap 500k chars, truncation clean via clamp post-join), `app/core/storage/virus_scanner.py` (ABC + `MockVirusScanner` détectant EICAR signature + `NoOpVirusScanner` + `ClamAVScanner` stub activation prod différée, factory selon settings). `FileUploadService.upload` avec pipeline ordonné strict en 10 étapes pour court-circuiter vite sur rejets : (1) MIME annoncé ∈ whitelist 15 MIME → 415 `FILE_TYPE_NOT_ALLOWED`, (2) lecture streaming par chunks 8KB + cap 100MB interruption précoce → 413 `FILE_TOO_LARGE`, (3) SHA-256 calculé pendant la lecture (single pass), (4) détection magic-bytes sur 4KB + `mimes_compatible` tolérance alias → 415 `FILE_CONTENT_MISMATCH`, (5) dédup `(user, SHA)` via SELECT, (6) scan virus (fail-open sur scanner failed, fail-closed sur suspicious) → 415 `VIRUS_DETECTED`, (7) upload MinIO avant INSERT (orphelin storage tolérable), (8) INSERT DB pending status, (9) extraction `asyncio.to_thread` fail-safe (exception → status `failed` + upload reste OK), (10) commit + return. Router `POST /files/upload` multipart avec query `extract_text` + `scan_virus`, rate limit user-scope 20/h via `check_user_rate_limit` → 429 `RATE_LIMIT_ABUSE`. **Extension de `POST /projects/{id}/files` avec `upload_id` optionnel** mutuellement exclusif via `@model_validator` Pydantic (402 `VALIDATION_ERROR` si les deux), mode attach copie storage_key/size/mime + `FileUploadService.mark_attached` pour trace forensic `attached_to_kind='project_file'`. Helper `_derive_project_file_type(mime)` dérive auto `image/pdf/doc/xls/ppt/audio/video/other` si `file_type` absent. **69 nouveaux tests** (20 mime_detector avec ZIP builder OOXML réel + 11 text_extractor avec PDF/DOCX minimaux construits en mémoire + 6 virus_scanner MockScanner EICAR + factory + 12 FileUploadService avec `_FakeUploadFile`/`_FakeDB` + 8 router TestClient + 6 projects_files upload_id + 6 intégration transverse). **477/477 verts + 3 skipped**, 0 régression, 2 runs back-to-back exit 0. **9 nouveaux settings** (`files_max_upload_bytes=100MB`, `files_allowed_mimes` liste 15 MIME, `files_extraction_max_chars=500_000`, `files_upload_rate_limit_per_hour=20`, `files_presigned_ttl_seconds=1800`, `virus_scan_enabled=True`, `clamav_host=""`, `clamav_port=3310`). 2 nouvelles dépendances (`python-multipart>=0.0.12` requis par FastAPI UploadFile + `pypdf>=5,<6` pure Python). Alembic env étendu (UploadedFile ajouté). | ✅ | 2026-04-24 |
| D2.5 | Endpoint `GET /files/{upload_id}` + `ProjectFileResponse` enrichi | Mini-extension contractuelle pour débloquer la session **D2 front Project Files** : Flutter doit poller `chunks_indexed_at` après upload PDF/DOCX/TXT/MD pour basculer son badge UI « Indexation… » → « Prêt RAG ». 4 fichiers backend modifiés. (1) `app/features/files/schemas.py` : ajout `chunks_indexed_at: datetime \| None = None` à `UploadedFileResponse` (sentinelle one-shot). (2) `app/features/files/router.py` : nouveau endpoint `GET /files/{upload_id}` retournant `NexyaResponse[UploadedFileResponse]` enrichi via `FileUploadService.get_for_user` (404 IDOR-safe), presigned URL régénérée à la volée TTL 30 min, **non rate-limité** (lecture O(1) PK + polling client borné). (3) `app/features/projects/schemas.py` : `ProjectFileResponse` enrichi avec 3 champs optionnels `presigned_url` / `upload_id` / `chunks_indexed_at` (default None pour rétro-compat). (4) `app/features/projects/router.py` : 2 nouveaux helpers — `_project_file_to_response` (single, POST) + **`_project_files_to_responses` (bulk anti-N+1, GET liste)** qui pour N pfiles fait 1 SEULE SELECT `WHERE attached_to_id IN (...)` puis N appels presigned locaux ~1 ms. **13 nouveaux tests** dans `tests/features/{files,projects}/` (6 GET /files/{id} : 200 happy + envelope + presigned + chunks_indexed_at null/ISO + 404 IDOR + 422 UUID malformé + 401 sans JWT + presigned URL régénérée à chaque appel ; 7 enrichissement helpers : single signe presigned + recopie chunks_indexed_at + None si legacy + None si worker D4 pas fait + bulk court-circuit empty + **`db.execute.await_count == 1` strict pour 10 pfiles** + mapping correct sans cross-contamination + presigned UNIQUEMENT pour pfiles avec storage_key). 13/13 verts en 46 s. Aucune nouvelle dep, aucune migration DB, aucun setting nouveau. Front D2 désormais débloqué. | ✅ | 2026-05-04 |
| E4 | Watermark visuel NEXYA sur images générées | Logo oiseau bleu (`logo_nexya.png` → `app/static/nexya_watermark.png`) overlay Pillow CPU-side bottom-right, scale 12 % largeur, opacity 70 %, singleton logo process-wide. Module `app/features/images/watermark.py` avec `apply_nexya_watermark(bytes, mime) -> (bytes, applied)` fail-safe absolu (exception → retour image originale + log, **jamais** bloquer `/image/generate`) + skip auto si image < 256 px (illisible) + constante `WATERMARK_VERSION = "v1-oiseau-bleu-2026-04"` + `reset_logo_cache_for_tests()`. Nouveau paramètre `POST /image/generate` body `remove_watermark: bool = False` avec gate : Free qui tente `True` → **403 `PLAN_REQUIRED`** avant tout appel LLM (économie facture immédiate) ; Pro → watermark non appliqué + metadata tracé. Metadata `library_items.metadata_json` enrichi `{has_watermark, watermark_version, no_watermark_was_requested}`. Response enrichie globalement `{watermark_applied, watermark_version}`. Format sortie préservé selon input (PNG RGBA, JPEG RGB quality 88, WEBP quality 88). Fix technique : `GeneratedImage` étant frozen dataclass, liste parallèle `final_b64_per_image` pour stocker les base64 watermarkés au lieu de muter l'objet. **1 setting pricing `# TODO(Ivan): provisoire`** : `image_no_watermark_price_multiplier=2.0` (ratio prix sans/avec, prépare wallet v2 sans l'implémenter) + 2 settings techniques (`image_watermark_scale_ratio=0.12`, `image_watermark_opacity=0.70`). Aucune nouvelle dépendance (Pillow déjà installé E2). Aucune migration DB (réutilise `library_items.metadata_json` existant C3). 28 nouveaux tests (17 watermark pur Pillow : PNG/JPEG/WEBP preserved, skip <256 px, scale/opacity override, format unknown→PNG fallback, logo singleton cache-hit, fail-safe bytes corrompus + logo missing, WATERMARK_VERSION exposée, frontière 256 px exacte, scale effectif larger image = larger logo, JPEG=RGB pas d'alpha artifact, PNG preserves alpha ; 11 intégration `/image/generate` : Free watermark appliqué + metadata tracé, Pro `remove_watermark=True` retiré + metadata tracé, Free `remove_watermark=True` → 403 PLAN_REQUIRED, Pro default garde watermark, multi-images 3 chacune watermarkée, base64 response contient watermark incrusté, fail-safe image originale si watermark crash, library_ids peuplé, response shape new fields, 422 sur `remove_watermark` non-bool, library save failure keeps watermark). **Fix technique side-quest** : monkeypatch dans tests doit patcher `app.main.get_ai_router` / `get_budget_tracker` / `get_moderation_service` directement (pas juste les modules source) car `main.py` importe les symboles at-import-time via `from ... import ...`. Hors scope (sessions futures) : C2PA manifeste signé cryptographiquement (E4.5 manuelle, requires Ivan génère clés X.509 via `openssl req -x509 ...` ou Adobe Content Credentials, AI Act UE obligation août 2026), watermark PDF/DOCX/PPTX (Phase 7-8 Nexya Studio export), retrait watermark via wallet credits (V2 pricing), custom watermark user-uploaded (Phase 20 Enterprise), watermark sur images user-uploadées (hors scope — on ne marque QUE les générations IA). Mémoires à consulter pour la suite : `project_nexya_watermark_export_v2.md` (C2PA + formats doc) + `project_nexya_pricing_model_v2.md` (wallet credits). | ✅ | 2026-04-24 |

**Livrable bloc E :** photo plat → « qu'est-ce que c'est » → réponse + lecture audio. Image générée → watermark C2PA vérifié via `c2patool`.

---

## **BLOC F — Phase 8 (Planner + Notifications push + Deep linking)**

**Sessions : 4 (F1, F2, F2.5, F3)** | **Heures : ~33** | **% du restant : 5.7 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| F1 | Planner Scheduler — CRUD tâches planifiées + worker arq dispatch/execute/cleanup | Tables `scheduled_tasks` + `scheduled_task_results` (migration 014), 8 endpoints `/tasks/*` (POST, GET list paginée keyset+filtre status, GET id, PATCH, DELETE 204, POST pause/resume, GET results), 4 schedule_type Pydantic discriminés v2 (once/interval_minutes/daily/weekly UTC, helper `compute_next_run`), workers arq : `dispatch_due_tasks` cron chaque minute (`SELECT FOR UPDATE SKIP LOCKED` concurrence multi-workers), `execute_scheduled_task` (retry transient 5min × max_retries=2), `cleanup_old_task_results` quotidien 04:23 UTC rétention 30j, quota Free=3/Pro=50 TODO(Ivan) provisoire, 67 tests | ✅ | 2026-04-24 |
| F2 | Notifications push FCM + tools LLM function calling | **FCM mock-first** : ABC `FCMProvider` + `FirebaseFCMProvider` HTTP v1 OAuth2 service account JSON (via `google-auth`) + `MockFCMProvider` accumulateur + factory singleton `get_fcm_provider()` avec warning unique boot si clé vide, hiérarchie erreurs typées (`FCMUnregisteredError` / `FCMInvalidArgumentError` / `FCMUnavailableError`). **Hook worker Planner** : `_send_task_push_notification` fail-safe post-`INSERT scheduled_task_results` (payload `{task_id, status, deep_link: nexya://task/{id}}`, body tronqué 140 chars sur `fcm_body_preview_max_chars`, `asyncio.gather` parallèle, soft-delete auto des tokens `UNREGISTERED` via `auth_service.remove_invalid_token`, log forensic `planner.push.sent`). **Tools LLM** : module `app/ai/tools/` avec `ToolDefinition` + `ToolRegistry` + 4 tools Planner (`create_task`, `list_tasks`, `update_task`, `pause_task`) + orchestrateur `run_with_tool_rounds` (cap `chat_max_tool_rounds=5` anti-boucle, format messages `assistant` + `user` tool results, supporte multi-deltas `ToolCallDelta` pour providers qui streament args partiellement). **Extension ABC** : `ChatCompletionRequest.tools`, `ChatChunk.tool_call`, `FinishReason.TOOL_CALLS`, `MockChatProvider(scripted_tool_call=…)`. **`StreamContext.tools` pass-through** + SSE `event: tool_call` émis par `_run_link` avant chaque `chunk`. **Registry init** au lifespan via `register_planner_tools()`. 41 tests F2 (6 FCM mock + 4 FCM runtime + 8 tools registry + 13 tools orchestrator + 3 mock ChatProvider tool_call + 7 planner push hook). Auth helpers `list_active_device_tokens` + `remove_invalid_token`. Nouvelle dépendance `google-auth>=2.30,<3`. | ✅ | 2026-04-24 |
| F3 | Notifications multi-canaux (push FCM + email) + préférences + timeline + unsubscribe one-click | **`NotificationDispatcher` dual-channel** : lookup préférence user (`NotificationPreferencesService._get_channel_for_category` avec defaults en Python, pas en SQL), tentative push FCM via `asyncio.gather` parallèle + soft-delete auto des tokens `UNREGISTERED` (hérité F2), fallback email auto si (push KO global **ET** `notification_fallback_email_enabled=True`) **OU** si 0 device actif **OU** si `channel_preference in {email, both}`. **Migration 015** avec 2 tables : `notifications` (17 cols, 3 index partiels dont `user_unread` pour badge non-lus) + `notification_preferences` (PK composite `(user_id, category)`, 1 index partiel `WHERE channel != 'none'`). **5 catégories RGPD** : `tasks/payments/security/digest/product` (mapping sémantique via `data_json.subtype` pour rétrocompatibilité `NotificationType` Flutter `update/feature/tip/promo/plannerTask`). **4 templates Jinja2** : `task_reminder` / `task_completed` / `payment_confirmed` / `account_security_alert` + partiel réutilisable `_layout_footer.html/.txt` injecté via `{% include %}` dans tous les templates (y compris `password_reset` existant). **Endpoints** : `GET /notifications` (keyset paginé + filtres `unread_only`/`category`), `POST /notifications/read` (bulk IDOR-safe idempotent), `DELETE /notifications/{id}` (404 IDOR-safe), `GET/PUT /user/notification-preferences` (namespace aligné `/user/profile` cohérent avec A1, 5 catégories toujours présentes avec defaults injectés côté service). **Endpoint public** `POST /notifications/unsubscribe/{token}` (sans auth — JWT RS256 purpose=`email_unsubscribe` TTL 365j + rate limit IP 10/h anti-brute-force + catégorie `security` refusée 400 `UNSUBSCRIBE_SECURITY_REFUSED` par obligation légale). **Fail-safe absolu** du dispatcher (`NotificationDispatcher.dispatch` ne raise JAMAIS — appelé depuis worker arq qui ne doit pas crasher). **Migration worker scheduler** : `_send_task_push_notification` F2 supprimée, remplacée par `_dispatch_task_notification` qui délègue au dispatcher. **3 nouvelles exceptions** (`UnsubscribeTokenInvalid` / `UnsubscribeTokenExpired` / `UnsubscribeSecurityRefused`). **5 settings F3** (fallback_enabled, max_attempts_push, unsubscribe_token_ttl_days, frontend_unsubscribe_url, unsubscribe_rate_limit_per_hour). 80 tests F3 : 8 unsubscribe tokens + 12 preferences + 15 service + 14 dispatcher (cœur : push-only/email-only/both/none + fallback push-all-fail → email + fallback disabled → skipped + 0 tokens → email direct + UNREGISTERED soft-delete + stringify data + security no-unsubscribe_url + reminder template + digest no template + exception-fail-safe + prefs lookup failure-safe) + 21 router + 10 scheduler migration. Aucune nouvelle dépendance pip. | ✅ | 2026-04-25 |

| F2.5 | Wiring tools LLM dans les 4 providers réels (DETTE F2 fermée) | OpenAI/Qwen (compat OpenAI) : `tools` + `tool_choice="auto"`, parse `delta.tool_calls` accumulés par index, finish `tool_calls` → TOOL_CALLS. Anthropic : helper `_to_anthropic_tools` (`parameters`→`input_schema`, drop wrapper `function`), parsing events typés `content_block_start`/`content_block_delta`(input_json_delta)/`message_delta` stop_reason=`tool_use`. Gemini : helper `_to_gemini_tools` (wrap `function_declarations`), parsing one-shot `chunk.candidates[0].content.parts[i].function_call`, helper `_gemini_args_to_json` (dict / Struct protobuf / None), force `FinishReason.TOOL_CALLS` si function_call vu. `ExpertConfig.tools_allowed: bool=True` (False sur `medicine`/`legal`). Router `/chat/stream` : injection `tool_registry.build_openai_tools()` dans `StreamContext.tools` quand `settings.tools_enabled_in_chat AND config.tools_allowed`, propagé aux 2 modes (legacy + persisté). Setting kill-switch `tools_enabled_in_chat: bool = True` (canary release prod). 27 nouveaux tests F2.5 verts (22 providers + 5 router injection), 0 régression réelle (3 fails pré-existants documentés : 2× G1 obsolètes, 1× flaky). Frontend F3 « chat-crée-task » désormais débloqué. | ✅ | 2026-04-25 |

| F1.5 | Range schedules Planner — 3 nouveaux schedule types (full-stack) | Première feature future promue depuis `project_nexya_future_features.md`. 3 classes Pydantic `WeeklyRangeConfig` (range continu jours de semaine, `start>end` = wrap weekend) / `MonthlyRangeConfig` (range continu jours du mois, `start<end` strict) / `MultiWeekdayConfig` (2-6 jours non-continus, liste triée+dédupliquée), `ScheduleType` 6→9 valeurs, union `ScheduleConfig` étendue. Migration `023_extend_range_schedule_types.py` (CHECK `ck_tasks_schedule_type` 9 types). `CheckConstraint` ORM `models.py` corrigé (était figé sur 4 types — dette F0.5 résorbée). `scheduler.py` : 3 branches `compute_next_run` + 4 helpers (`_next_in_weekday_set` factorisé, wrap weekend, clamp dernier jour du mois via `calendar.monthrange()` — bug spec février corrigé). 0 modif router/service/worker. 88 tests planner ciblés + 57 régression verts, 0 régression. Frontend full-stack livré en parallèle (enum +3, `TaskModel` +5 champs, 3 blocs UI form, 10 ARB FR+EN, 414/414 tests Flutter). 0 nouvelle dep, 0 nouveau setting, 0 changement pricing. | ✅ | 2026-05-21 |
| F1.6 | `yearly_range` — 10ᵉ schedule type, mois figé (full-stack) | Extension de F1.5 sur retour produit Ivan. `monthly_range` se répétait tous les mois sans choix de mois → `yearly_range` ajoute « chaque année, en janvier, du 15 au 30 » (récurrence annuelle, mois figé). Classe Pydantic `YearlyRangeConfig` (`month` 1-12 + `start_day`/`end_day` + hour/minute), validation `start_day < end_day` strict + bornes valides pour le mois (30/02 et 31/04 rejetés, 29/02 accepté + clampé en année non bissextile). Factorisation `_MAX_DAYS_PER_MONTH` + `_ensure_day_in_month()` partagés avec `YearlyConfig`. `ScheduleType` 9→10, migration `024` (CHECK 10 types), `CheckConstraint` ORM 9→10, `scheduler.py` helper `_make_yearly_range_candidate` (mois figé, clamp `calendar.monthrange()`, année courante puis suivante). 0 modif router/service/worker. 108 tests planner ciblés + 57 régression verts. Frontend full-stack en parallèle (`TaskModel` +`month`, picker de mois + jours bornés au mois, libellé `monthly_range` clarifié « Tous les mois (du X au Y) », 420/420 tests Flutter). 0 nouvelle dep, 0 nouveau setting, 0 changement pricing. | ✅ | 2026-05-21 |

**Livrable bloc F :** « Rappelle-moi maths demain 18h » → notification 18h00 lendemain selon préférences user (push si actif, email en fallback, les deux si choisi) → clic push ouvre la tâche, clic email redirige vers deep link app ou web. **Tools LLM** : un user qui demande « rappelle-moi de prendre mon médicament demain à 8h » dans `/chat/stream` (expert `general`) → Gemini reçoit les 4 tool definitions Planner → décide d'appeler `create_task` → SSE `event: tool_call` émis → orchestrateur exécute le tool → ligne créée en DB `scheduled_tasks` → résultat ré-injecté → Gemini répond « C'est noté, je te rappellerai chaque jour à 8h » → SSE `event: done`.

---

## **BLOC G — Phase 9 (7 Modes Experts RAG spécialisés)**

**Sessions : 7** | **Heures : 70** | **% du restant : 12.3 %**

> ⚠️ **Dépend de Phase 6 (RAG)** — ne pas démarrer avant le bloc D terminé.

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| G1 | Expert Langues — INFRA livrée, SCOPE corpus Tatoeba **abandonné** post-validation | Code livré : `GeminiEmbeddingsProvider` 768 dim avec switch **Vertex AI/AI Studio** (settings `gemini_use_vertex` + `gcp_project_id` + `gcp_region`, ADC via `gcloud auth application-default login`) + migration 016 `expert_corpus_chunks` + HNSW + package `app/features/experts/` + hook `/chat/stream` ordre `memory→corpus→system` + script Tatoeba idempotent + 30 questions blind test Gemini-as-judge. **Validation end-to-end (2026-04-24)** : 14 migrations 003→016 appliquées sur vraie DB, `pytest` full suite × 2 (1050 passed après fix 1 heuristique + 1 flaky pré-existant non-G1), 10 000 paires Tatoeba ingérées via Vertex (vérifié `SELECT COUNT(*) = 10000`, 9 paires de langues), **blind test 13/30 wins RAG (43 %) → ÉCHEC du seuil 24/30**. **10 bugs trouvés et fixés** : (1) migration 005 down_revision pré-G1 héritage, (2) heuristique eng-fra "translate to French", (3) circular import streaming↔engine via TYPE_CHECKING, (4) modèle Gemini renommé `text-embedding-004` → `gemini-embedding-001`, (5) `--dry-run` consommait quota, (6) ProactorEventLoop manquant ingestion script, (7) switch Vertex AI absent, (8) `output_dimensionality=768` manquait Vertex, (9) ProactorEventLoop manquant `run_eval.py`, (10) `run_eval` ignorait `gemini_use_vertex`. **Bug cosmétique #11** : counter `inserted=-100` vs `seen=10000` (DB OK, accounting buggy). **Décision produit consolidée avec Ivan** : abandon scope « Expert Langues via Tatoeba » — Gemini 2.5 Pro brut couvre déjà excellemment FR/EN/ES/PT, RAG = redondant ; les langues vernaculaires camerounaises (Duala, Bassa, Medumba, Fulfulde, Ewondo, Bamiléké) seront couvertes par **bloc H fine-tuning Gemma** où le RAG ne suffit pas (corpus ne contient pas ces langues). **Cleanup post-décision** : `corpus_enabled = False` posé sur l'expert `language` dans `app/ai/experts.py` (commentaire détaillé), `DELETE FROM expert_corpus_chunks WHERE expert_slug = 'language'` à exécuter manuellement par Ivan (Claude Code a bloqué le bulk DELETE en garde-fou). **Infra technique CONSERVÉE** : table `expert_corpus_chunks`, `GeminiEmbeddingsProvider` (mode Vertex opérationnel, pattern réutilisable), pipeline ingestion idempotent, helper `build_expert_corpus_context` — **réutilisable telle quelle pour G2 Cuisine, G4 Ingénierie, G6 Informatique** (où le RAG aura un sens face à des LLMs qui manquent de données spécialisées). **Leçon produit** : le RAG n'apporte de valeur que quand le LLM manque de données spécifiques mais maîtrise suffisamment le domaine — pas sur les langues qu'il connaît parfaitement (G1 langues majeures), pas sur les langues qu'il ne connaît pas du tout (camerounaises → fine-tuning seul peut). | ✅ infra livrée / scope corpus abandonné | 2026-04-24 |
| G2 | Expert Cuisine | Collecter recettes camerounaises/africaines (sources licites), indexer pgvector, évaluation 30 questions | ❌ | — |
| G3 | Expert Studio créatif | Collecte prompts d'image (PromptHero) + théorie design, indexer, évaluation | ❌ | — |
| G4 | Expert Ingénierie | Normes ISO publiques + formules, indexer, évaluation | ❌ | — |
| G5 | Expert Productivité | GTD/Pomodoro/OKR, indexer, évaluation | ❌ | — |
| G6 | Expert Informatique | Docs officielles (Python, Flutter, Rust, Go…), indexer, évaluation | ❌ | — |
| G7 | Expert Sciences/Maths + transversal `expert_slug` | Khan Academy + Wikipedia formules + exercices, indexer, évaluation. Plus : `/chat/stream` accepte `expert_slug`, charge system prompt + retrieve corpus | ❌ | — |

**Livrable bloc G :** « Cuisine » + « comment faire un ndolè » → réponse spécifique camerounaise avec sources, supérieure à généraliste.

**Stratégie révisée 2026-04-24 (post-validation G1) :** G1 Expert Langues abandonné comme cas d'usage RAG (Gemini 2.5 Pro brut excellent sur FR/EN/ES/PT, langues camerounaises → bloc H fine-tuning Gemma). **G2 Cuisine** (recettes camerounaises authentiques) reste **le différenciateur RAG day-one** car Gemini ne connaît pas le ndolè / eru / koki / fufu. G3-G7 livrables progressivement V1.5/V2 selon priorité produit. Le pattern technique G1 (table + provider + pipeline + hook) est réutilisable tel quel pour G2-G7. Voir `CLAUDE.md` §15 entrée G1 du 2026-04-24 pour le post-mortem détaillé et la leçon produit fondamentale.

---

## **BLOC H — Phase 10 (Modèles locaux Gemma + fine-tuning + MLOps)**

**Sessions : 8** | **Heures : 80** | **% du restant : 14.0 %**

> ⚠️ **Phase la plus exigeante.** ROADMAP : « ne pas démarrer avant fast.ai partie 1 finie » (~30-40 h d'apprentissage ML préalable).
> ⚠️ **Peut être considéré V2** — pas bloquant pour le lancement.

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| H1 | Choix base + dataset preparation | Choisir Gemma 2 9B ou Mistral 7B Instruct, télécharger HF, valider local (`transformers` ou Ollama), préparer dataset ChatML JSONL (~10k-50k exemples) | ❌ | — |
| H2 | Versioning DVC + premier fine-tune LoRA | Versionner datasets DVC (push S3/MinIO), fine-tune LoRA Colab Pro+ (A100, ~10 $/run) | ❌ | — |
| H3 | Suite évaluation CI | 100-500 prompts, métriques (perplexité, MMLU, qualité), bloque déploiement si régression | ❌ | — |
| H4 | Red-teaming | 50 prompts adversariaux, harness reproductible | ❌ | — |
| H5 | Quantization + déploiement | Quantifier GGUF Q4_K_M, déployer Ollama VPS GPU (Hetzner ~150 €/mois) ou vLLM RunPod | ❌ | — |
| H6 | Model registry + LocalProvider | Enregistrer modèle dans HF Hub privé ou MLflow, ajouter `LocalProvider` à la Couche IA | ❌ | — |
| H7 | Drift detection | 50 prompts canaris hebdo, alertes si déviation | ❌ | — |
| H8 | Mode offline mobile (optionnel) | Gemma 2B quantifiée via `flutter_gemma` pour mobile-embarqué | ❌ | — |

**Livrable bloc H :** modèle `nexya-cuisine-v1` bat GPT-4o-mini sur 50 questions cuisine camerounaise, hébergé infra dédiée, accessible via router, suite évals + red-team verts.

---

## **BLOC I — Phase 11 (Subscriptions + Paiements multi-pays)**

**Sessions : 4** | **Heures : 40** | **% du restant : 7.0 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| I1 | Tables subscriptions + plans + endpoints status | Table `subscriptions`, table `plans` (prix par pays PPP), `GET /subscriptions/status`, `POST /subscriptions/checkout`, `POST /subscriptions/cancel`, middleware quota custom par plan refus 402 | ❌ | — |
| I2 | Webhook RevenueCat + Stripe | `POST /webhooks/revenuecat` (HMAC + idempotence) → met à jour `users.plan`, `POST /webhooks/stripe`, tests | ❌ | — |
| I3 | Webhooks Mobile Money Afrique | `POST /webhooks/cinetpay`, `POST /webhooks/notchpay`, `POST /webhooks/wave`, `POST /webhooks/mpesa`, providers correspondants | ❌ | — |
| I4 | Job arq daily downgrade + tests E2E paiement | Job arq daily : downgrade users `plan_expires_at < now()`, tests sandbox Apple iAP + Mobile Money Orange Cameroun (compte réel test) | ❌ | — |

**Livrable bloc I :** sandbox Apple iAP → backend webhook → `users.plan = 'pro'` → quotas débloqués. Test Mobile Money Orange Cameroun fonctionnel.

---

## **BLOC J — Phase 12 (RGPD + AI Act backend)**

**Sessions : 1** | **Heures : 10** | **% du restant : 1.8 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| J1 | Export RGPD + consent + registre IA + purge | `GET /rgpd/user/data-export` (ZIP 23 fichiers JSON complets, presigned URLs blobs MinIO 7j TTL, IP anonymisée /24, 0 password_hash leak), `POST /rgpd/user/account/delete-request` workflow 2-step Article 17 (soft-delete + grace 30j + cron purge `purge_deleted_accounts` 03:47 UTC SELECT FOR UPDATE SKIP LOCKED batch=50 + cascade SQL 22 tables + suppression blobs MinIO fail-safe), table `consent_log` (preuve juridique horodatée document_hash SHA-256 figé anti-modification rétroactive) + `deletion_requests` (queue purge différée idempotence stricte index unique partial), enrichissement `ai_calls` Article 13 AI Act (legal_basis + data_categories + retention_until), `GET /rgpd/admin/ai-act-registry` CSV BOM UTF-8 / JSON via `require_admin` ACL email-based, 5 endpoints publics + 1 admin, ~67 tests J1, 5 settings dont 2 TODO(Ivan) provisoire (`rgpd_deletion_grace_period_days=30` + `rgpd_export_max_size_bytes=100MB`) + production safety guard fail-fast en prod si `rgpd_admin_emails == []`, 2 templates email FR (data_export_ready + account_deletion_scheduled). Aucun impact API Flutter (5 endpoints documentés pour session frontend dédiée future). | ✅ | 2026-04-26 |

**Livrable bloc J :** export complet données fonctionne, suppression compte avec purge effective J+30, registre IA documenté.

---

## **BLOC K — Phase 13 (Observabilité prod)**

**Sessions : 2** | **Heures : 20** | **% du restant : 3.5 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| K1 | OpenTelemetry + Sentry + Prometheus | 3 piliers complémentaires fail-safe absolu : OTel auto-instrumentation 5 couches (FastAPI/SQLAlchemy/httpx/Redis/asgi) + spans manuels critiques (StreamHandler, run_with_tool_rounds, NotificationDispatcher, workers arq) + export OTLP/HTTP fail-open ; Sentry env-aware (DSN vide = init jamais appelé) avec scrubber A3 ponté par alias public + filtre `CancelledError`/`NexYaException` ; Prometheus registry custom + 13 métriques NEXYA (chat, tools, notifications, workers, cache, breaker) + endpoint `/metrics` auth token + `/observability/status` JSON + production safety guard étendu (`PROMETHEUS_SCRAPE_TOKEN` obligatoire en prod, fail-fast au boot) ; injection `trace_id`/`span_id` OTel dans structlog. Feature flags (GrowthBook) reportés Phase 13/19 — pas day-1. | ✅ | 2026-04-26 |
| K2 | Dashboards Grafana + alertes Prometheus | 5 dashboards JSON provisionnés (`nexya-overview`, `nexya-ai`, `nexya-tools-notifications`, `nexya-workers`, `nexya-self`) avec UIDs stables, schemaVersion 39+, `allowUiUpdates: false` (single source of truth Git) ; 6 alertes calibrées format Prometheus rules natif compatible Grafana 10+ alerting (5xx > 1 %, p95 chat > 5s, breaker open, FCM failures > 5 %, arq failures > 10 %, coût IA > seuil USD/jour TODO(Ivan) provisoire) ; `docker/docker-compose.observability.yml` séparé (Prometheus 2.55.0 + Grafana 11.3.0, retention TSDB 7d en dev) ; tests JSON/YAML structure + métriques cross-check anti-dérive K1↔K2. AlertManager runtime déploiement (destination email/Slack) → L2 staging | ✅ | 2026-04-26 |

**Livrable bloc K :** tableau de bord unique répondant : actifs, dépensé IA, crash, latence p95, conversion paywall, NPS hebdo.

---

## **BLOC L — Phase 14 (CI/CD + Staging + Prod + WAF)**

**Sessions : 4** | **Heures : 40** | **% du restant : 7.0 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| L1 | CI/CD GitHub Actions + rollback | 4 workflows (`ci.yml` 6 jobs lint+typecheck mypy+security bandit/pip-audit+tests services pg/redis/minio+docker-build+migrations-check upgrade/down -1/up réversibilité, `release.yml` 4 jobs validate workflow_call + build-and-push GHCR 3 tags + create-github-release + notify, `codeql.yml` weekly Monday 6h UTC + main, `dependabot-auto-merge.yml` patch+minor avec CI vert), `dependabot.yml` 3 updaters (pip+docker+github-actions) weekly, `release.yml` config auto-changelog labels-based, `.github/branch-protection.md` doc UI, 3 scripts shell strict bash (`rollback.sh` 80 lignes dry-run + smoke + restore + cleanup .bak 5min, `smoke_test.sh` 40 lignes healthz/ready/metrics/observability + register staging-only, `release.sh` 60 lignes semver bump + tag annotated + push), Makefile 16 targets avec `make help` auto-doc + `make ci` enchaîne lint+typecheck+security+test, `.pre-commit-config.yaml` 7 hooks (ruff + check-yaml/toml/large/conflict/private-key + EOF/whitespace) opt-in Ivan, 5 nouvelles dev deps (mypy/coverage/bandit/pip-audit/pre-commit), `tool.ruff` ignore étendu V1 (B008 Depends FastAPI + TC001/2/3 invasif + F841/SIM/B9xx cosmétiques) + auto-format 226 files + 1 vrai bug fix `EmbeddingsError` import manquant, `tool.mypy` `ignore_errors=true` sur `app.*` V1 (78 erreurs strict historiques → TODO Phase 19), `tool.coverage` `fail_under=60` provisoire (TODO V2:75% V3:80%), `tool.bandit` exclude tests+migrations + skips B101 assert, ~28 nouveaux tests structure YAML+bash, security-scan `continue-on-error` V1 (8 CVEs pypdf+pytest pré-existants documentés non-bloquants), `pip-audit --skip-editable`, principle of least privilege permissions partout, 0 image Docker `:latest`, 0 action `@main`/`@latest`, whitelist 6 orgs GHA. | ✅ | 2026-04-26 |
| L2 | Staging Hetzner + Caddy + premier déploiement | Provisionner staging Hetzner CX32 (clés SSH, ufw, fail2ban), installer Docker + Caddy reverse proxy + TLS auto Let's Encrypt, docker compose prod (api + arq worker + nginx ou Caddy), domaine `staging.api.nexya.ai`, **premier déploiement manuel pour valider la chaîne avant CI/CD** | ❌ | — |
| L3 | Prod Hetzner + Cloudflare WAF + go-live | Provisionner prod Hetzner CX42/CCX13, domaine `api.nexya.ai`, Cloudflare DNS + TLS + WAF OWASP + rate limit IP + bot fight + anti-DDoS L7, **cérémonie go-live release `v1.0.0`** (release notes GitHub, tag Git signé, monitoring renforcé 48 h post-deploy), **soft launch progressif** via Cloudflare load balancing (10 % → 50 % → 100 % sur 72 h) | ❌ | — |
| L4 | Backups + restore + versioning + runbooks + status page | Backup quotidien PG → S3 chiffré + WAL streaming, test restore mensuel automatique, versioning API `/v1/` + politique deprecation 6 mois, endpoint `/version/min-supported` forced-update mobile, runbook incidents Markdown, postmortem template, migrations prod-safe (add nullable → backfill → set not null), **status page publique `status.nexya.ai`** (BetterStack ou Instatus, monitors auto sur `/healthz` + `/ready`) | ❌ | — |

**Livrable bloc L :** push `main` → 12 min plus tard prod, monitoré, rollback en 1 commande. Backup restauré avec succès en < 30 min. Cloudflare bloque attaque test.

---

## **BLOC M — Phase 19 (Pentest + scaling + conformité avancée)**

**Sessions : 3** | **Heures : 30** | **% du restant : 5.3 %**

> 🔒 **Avant 10k users payants** — pas urgent au lancement, mais bloquant pour scaler sereinement.

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| M1 | Pentest externe + correction failles | Pentest freelance (Yes We Hack, Yogosha, ~1500-3000 €), couvrir backend + mobile, corriger toutes failles critiques + hautes, page `/security` + bug bounty informel | ❌ | — |
| M2 | Scaling DB + CDN images | Hetzner CCX23 (16 vCPU 64 Go) + replica lecture asynchrone, balancing read/write côté SQLAlchemy, CDN images MinIO/R2 derrière Cloudflare, signed URLs courte durée | ❌ | — |
| M3 | Audits RGPD/AI Act + cellule de crise | DPIA si données sensibles, désigner DPO si seuils atteints, audit AI Act validé, cellule de crise documentée Notion (data breach, délai notif CNIL 72 h, com users), audit accessibilité externe | ❌ | — |

**Livrable bloc M :** rapport pentest avec failles critiques/hautes corrigées, DPIA documentée, audit AI Act validé, infra scalée jusqu'à 10k users sans incident.

---

## **BLOC N — Tâches transversales backend**

**Sessions : 5** | **Heures : 48** | **% du restant : 8.3 %**

> Petites pièces dispersées qui ne rentrent dans aucune phase.

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| N1 | Endpoints manquants divers | 4 endpoints livrés : `POST/DELETE /chat/messages/{message_id}/feedback` (table dédiée `message_feedback` + UPSERT atomique `pg_insert.on_conflict_do_update` + IDOR-safe owner check JOIN + DELETE idempotent anti-énumération), `GET /voice/list` (catalogue 6 voix NEXYA branded `aurora`/`memora`/`soleil`/`sagesse`/`eron`/`nyanga` constante Python alignées Flutter, auth requise mais PAS Pro-only, Cache-Control public 1h), `GET /models` (aggregation runtime `chat_providers` + `image_providers` du LlmRouter, mapping `_MODEL_DISPLAY_NAMES` 25+ modèles + tier flash/pro/ultra dérivé max_context_tokens, `is_default_for` croisé EXPERT_REGISTRY 11 experts, Mock filtré en prod, Cache-Control private 5min), `POST /suggestions` (formulaire user → équipe NEXYA, 4 types bug/feature/expert_domain/other, body 1-2000, rate limit 5/jour user-scoped, email fail-safe via Brevo/Mock à `feedback_team_email`, IP anonymisée /24 dans contexte template). Migration 018 = 2 tables (`message_feedback` UNIQUE composite + FK CASCADE × 2 + index `(message_id, rating)` pour agrégat N3 ; `user_suggestions` FK SET NULL RGPD-safe + 4 CHECK + index partiel `WHERE processing_status='open'` pour queue admin V2). 4 settings (`feedback_team_email=feedback@nexya.ai` recommandation Ivan créer alias avant prod, `suggestions_rate_limit_per_day=5`, `feedback_rate_limit_per_hour=60`, `models_endpoint_cache_ttl_seconds=300`). 0 pricing TODO. ~59 nouveaux tests N1 verts (12 feedback service + 8 feedback router + 5 voice/list + 5 voices_catalogue + 8 ai_models service + 4 ai_models router + 10 suggestions service + 4 suggestions router + 3 email template). 0 régression. | ✅ | 2026-04-27 |
| N2 | Tests unitaires + intégration manquants | 7 fichiers de tests livrés (~100 tests N2 verts) : `test_experts_registry.py` (31 — intégrité 11 expert_id slugs contrat API Flutter, champs essentiels non-vides, `get_expert_config` permissif, invariants safety-critical `medicine`/`legal` `tools_allowed=False`+disclaimer+temperature ≤ 0.2, Studio image-only, `full_chain` primaire+fallbacks, frozen dataclass), `test_llm_router.py` (24 — constructeur refus chat_providers vide + copie défensive, `resolve` happy/fallback general/studio raise/chaîne non-viable raise, `build_chain` skip provider non-enregistré + warning model hors supported, `resolve_image` happy/sans provider raise, introspection has/names triés, `build_default_router` mock-first identité usurpée, ChatResolution/ImageResolution frozen), `test_cost_tracker_extended.py` (10 — complète test_cost_tracker.py avec attempts+fallback_used forwardés, user_id=None bucket anonyme, `_jsonify` nested dict + UUID/Decimal via default=str + None→None, `_to_decimal` Decimal=identité, UPSERT déclenché pour cancelled, UPSERT skippé pour failed, `record_ai_call_background` retourne asyncio.Task), `test_auth_tasks_worker.py` (4 — cron `cleanup_refresh_tokens` retourne `{deleted: N}` depuis rowcount, fail-safe rowcount=None→0, SQL DELETE FROM refresh_tokens contient OR sur expires_at + revoked_at, constantes EXPIRED_RETENTION=1j/REVOKED_RETENTION=7j), `test_auth_flow_integration.py` (12 — smoke 10 endpoints auth montés, `/auth/refresh` délégation+propagation 401 AUTH_REFRESH_EXPIRED, `/auth/logout` décode JWT+délégation, `/user/profile` requiert auth, PUT profile/password/account délégation, POST/DELETE /user/device-token), `test_chat_stream_flow_integration.py` (9 — singletons `runtime.py` `get_ai_router`/`get_cost_tracker`/`get_stream_handler` + `reset_runtime_for_tests`, `/chat/stop` délégation `mark_cancelled` + auth requise, `/chat/reports` rate limit user-scoped puis ReportService + 409 `DUPLICATE_REPORT` propagé, smoke 4 endpoints), `test_planner_flow_integration.py` (10 — `compute_next_run` accepte ISO string `at` from Pydantic dump + schedule_type inconnu→None + `from_dt` naive coerce UTC + once passé→None + ISO malformé→None, POST pause/resume délégation, GET /tasks/{id}/results forward cursor+limit, smoke 5 endpoints, DELETE 204 sans body). Mock-first strict (`AsyncMock` + `app.dependency_overrides` + `monkeypatch.setattr`), aucun Postgres réel, aucun Redis réel. 0 régression sur les ~1422 tests pré-N2. | ✅ | 2026-04-27 |
| N3 | Évals IA en CI (suite reproductible) | Harness Python pur dans `tests/evals/` : `judge.py` ABC + `MockJudge` SHA-256 déterministe + `GeminiJudge` 2.5 Pro structured output (parser tolérant 3 passes). 5 catégories × 130 prompts versionnés YAML : `routing` (15 — pure introspection EXPERT_REGISTRY), `safety` (28 — refus prescriptions/actes nominatifs + jailbreaks), `format` (30 — code blocks, LaTeX, listes numérotées), `accuracy` (44 — faits vérifiables par expert), `identity` (18 — marque NEXYA jamais cassée). `runner.py` orchestre + `candidate.py` (Gemini SDK direct, system_prompt expert, temperature=0) avec mock_candidate auto si judge=mock (pipeline test sans facture). `baseline.py` snapshot JSON committé `tests/evals/baselines/baseline.json` avec commit_sha + date + judge_name + pass_rate/score_avg par catégorie ; `diff_vs_baseline` détecte régression en pp. `report.py` markdown + JSON. `cli.py` argparse complet (--judge, --category, --limit, --update-baseline, --threshold-pp, --md-out, --json-out). Workflow `.github/workflows/evals.yml` 2 jobs : `evals-pr` (mock judge, threshold 10pp, comment PR auto, bloquant si régression) + `evals-nightly` (cron 3h00 UTC, real Gemini judge, threshold 5pp, ouvre issue auto si régression). Skip gracieux si `GEMINI_API_KEY` absente. 3 settings (`evals_judge_model=gemini-2.5-pro`, `evals_regression_threshold_pp=10.0`, `evals_corpus_min_size=100`). 49 tests pytest verts (judge×16 + baseline×14 + runner+report×19), 0 régression. Coût : $0 PR (mock) + ~$30/mois nightly (real judge). Recommandation Ivan : créer `secrets.GEMINI_API_KEY` dans GitHub Settings AVANT premier nightly. | ✅ | 2026-04-27 |
| N4 | Tests de charge + Phase 18 backend mineur | **Volet A** : suite k6 reproductible dans `tests/load/` — 6 scénarios (`auth_burst` 50 RPS / `chat_stream_concurrent` 30 VUs SSE / `files_upload_concurrent` 20 VUs 1MB / `conversations_list_paginated` 100 RPS keyset / `metrics_endpoint` 200 RPS / `mixed_workload` 30 VUs ramping 60/30/10), lib partagée (`auth.js` token cache, `sse.js` parser NEXYA-aware, `metrics.js` Trends alignés K1), `thresholds.json` SLO codifiés (login p95 <500ms, chat total <30s, list <300ms, /metrics <100ms), `docker-compose.load.yml` stack éphémère (postgres pg16 + redis + minio + backend mock-first), `bootstrap.sh` + `teardown.sh` strict bash, `run.sh` orchestrateur (`--scenario`, `--no-teardown`, `--skip-bootstrap`), workflow `.github/workflows/load.yml` (workflow_dispatch + cron weekly Sunday 4h UTC, k6 via `grafana/setup-k6-action@v1`, fail job + open issue label `load-regression` si breach, upload artifacts 30j). **Volet B Phase 18** : `app/integrations/crisp_client.py` (ABC `CrispClient` + `RealCrispClient` httpx async POST `/v1/website/{id}/conversation` Basic Auth + `MockCrispClient` accumule calls + `force_fail` / factory mock-first auto si `CRISP_API_KEY` vide), migration 019 + ORM `HelpdeskEscalation` (5 categories CHECK, 4 severity, 4 status, 3 index partiels dont queue admin priorisée + crisp lookup), `CrispEscalationService.escalate` (INSERT row local → Crisp call fail-safe absolu → UPDATE `crisp_conversation_id` si succès, `should_escalate` Pro+high+kill-switch), `HelpdeskMetricsService.compute` (counts par status + median_resolved_age_hours via `percentile_cont` + oldest_open_age_hours + breakdown par catégorie), `GET /admin/helpdesk/metrics` admin J1 ACL, hook `_maybe_escalate_to_crisp` dans `core/errors/handlers.py` (fire-and-forget `asyncio.create_task` sur `PaymentFailedException`/`PaymentWebhookInvalidException`/`LlmUnavailableException` quand user Pro). 5 settings (3 Crisp + 2 load), 64 tests pytest verts (10 crisp_client + 18 helpdesk_service + 3 helpdesk_router + 33 load_thresholds). 0 régression. Coût V1 : Crisp = pricing externe (Ivan choisit), k6 GHA = ~10 min/run × manuel + weekly = marginal. **Recommandation Ivan** : créer `secrets.GEMINI_API_KEY` (déjà N3) + `secrets.CRISP_WEBSITE_ID/CRISP_API_KEY` (Phase 18) avant déploiement L2. | ✅ | 2026-04-27 |

| N5 | NPS endpoint + table `nps_responses` + admin metrics | **Volet A — Migration & ORM** : migration Alembic 020 `nps_responses` (id UUID PK gen_random_uuid + user_id UUID FK CASCADE + score smallint CHECK 0-10 + comment text nullable max 2000 + app_version text 32 + source enum `in_app_survey/email/admin_import` + sent_at_app timestamptz + created_at timestamptz default now() + idx composite `(user_id, created_at DESC)` pour rate limit lookup + idx partiel `WHERE source='in_app_survey'` pour summary admin). ORM `app/features/feedback/nps_models.py::NpsResponse` aligné. **Volet B — Schémas Pydantic** : `NpsSubmitRequest` (score 0-10 strict + comment? str≤2000 sanitizer A3 NFC + null bytes + zero-width + app_version regex `^\d+\.\d+\.\d+(\+\d+)?$` + sent_at_app ≤ now+5min anti future), `NpsSubmitResponse` (id, score, created_at, next_eligible_at), `NpsSummary` (count_total, count_promoter ≥9, count_passive 7-8, count_detractor ≤6, nps_score float = %prom - %détr, avg_score, last_30d_score, last_90d_score), `NpsResponseListItem` admin-only (id, user_id_hash sha256[:12] anonymisé RGPD, score, comment, app_version, source, created_at). **Volet C — Service** : `NpsService.submit(user, body, db)` rate limit Redis sliding window 1/30j scoped par user.id + INSERT + return next_eligible_at = last_submit + 30j ; `NpsService.compute_summary(date_from, date_to, db)` SQL `count(*) FILTER (WHERE score >= 9) AS promoter` etc. + window 30/90j ; `NpsService.list_responses(cursor, limit ≤ 50, score?, source?, db)` cursor-based DESC. **Volet D — Endpoints** : `POST /feedback/nps` auth requise (capture business event Sentry `nps_submitted` avec score+source — ne JAMAIS logger comment en clair), `GET /admin/nps/summary` ACL `require_admin` J1 + filtre date_from/date_to, `GET /admin/nps/responses` ACL admin + cursor + filtres ; tous routes avec OpenAPI tags `feedback` / `admin`. **Volet E — Rate limit** : helper `rate_limit_nps_per_user(user_id, days=30)` aligné pattern `rate_limit_abuse_reports` C2 (sliding window Redis avec TTL > 30j, retry_after en secondes dans body 429). **Volet F — Tests** : ~25 tests verts (Pydantic score 0/10/-1/11/null/string + comment XSS NULL bytes scrub + app_version regex + sent_at_app future > now+5min reject ; service submit happy + rate limit 429 retry_after exact + capture Sentry mocked ; admin summary calculation 0 row vs 100 rows + cohort filtré + IDOR-safe non-admin 403 ; list_responses pagination + filtre score≥9 + 0 PII clear dans payload). **Volet G — Setting** : `nps_rate_limit_days=30` dans `app/config.py` (PAS pricing TODO — c'est UX, pas coût). **0 nouvelle dep**. **0 setting pricing TODO**. **Backend Sentry K1 hook** : `capture_message("nps_submitted", level=info, tags={score, source})` au succès (le score remonte dans le funnel observabilité K2 dashboard `nexya-self`). Recommandation Ivan : créer `nexya-self` panel Grafana « NPS rolling 30d » via PromQL fed par Prometheus counter `nexya_nps_submitted_total{score_bucket}`. | ❌ | — |

**Livrable bloc N :** suite tests complète, évals IA bloquantes en CI, tests de charge documentés, support utilisateur structuré, **NPS in-app collecté côté backend** (consommé par Frontend N5).

---

## **BLOC O — Polish final & Documentation**

**Sessions : 2** | **Heures : 20** | **% du restant : 3.5 %**

| # | Session | Tâches | Statut | Fait le |
|---|---|---|---|---|
| O1 | OpenAPI/Swagger + Health check étendu + Headers sécurité | **Volet A OpenAPI** : `app/core/openapi/customizer.py` enrichit le schéma auto-généré FastAPI (OpenAPI 3.1) — `info.title/description FR/contact/license/termsOfService/x-logo`, `servers` dev+staging+prod placeholders, `tags` 20 entries avec descriptions FR détaillées (auth/user/chat/projects/library/files/voice/vision/memory/rag/ai_models/tasks/notifications/feedback/suggestions/rgpd/admin/helpdesk/observability/health), `securitySchemes.BearerAuth` HTTP bearer JWT + `securitySchemes.PrometheusToken` apiKey header `X-Prometheus-Token`, hook `app.openapi = lambda: customize_openapi(app)` après `include_router`. **Volet B Health étendu** : `app/core/health/version.py` `detect_version()` 3 fallbacks (env vars `APP_VERSION/APP_COMMIT_SHA` posés par CI/CD release → subprocess `git describe --tags` + `rev-parse HEAD` + `status --porcelain` → lecture `.git/HEAD` direct) retourne `VersionInfo(version, commit_sha[:40], tag, dirty, source)` ; `app/core/health/extended.py::ExtendedHealthService.compute(db, redis)` agrège `_VersionPayload` + `_DbPayload(status, latency_ms, last_migration via SELECT version_num FROM alembic_version)` + `_RedisPayload(status, latency_ms via PING)` + `_ArqPayload(queue_depth via ZCARD arq:queue)` + `uptime_seconds` (depuis `set_app_start_monotonic()` posé au lifespan startup), tous fail-safe par champ ; `/ready` réécrit pour exposer ce payload (status 200/503 backward-compat K8s, body étendu) ; nouvel endpoint public `GET /version` léger sans token (Flutter peut afficher la version backend). **Volet C Headers sécurité** : `app/core/security/headers.py::NexyaSecurityHeadersMiddleware` 4 presets `dev` (X-Content-Type-Options nosniff seul) / `staging` (CSP unsafe-inline + HSTS max-age=31536000 + X-Frame-Options DENY + Referrer + Permissions camera/mic/geo/payment/usb/magnetometer) / `prod` (HSTS preload includeSubDomains + COOP same-origin + CORP same-origin + CSP strict sans unsafe-inline + base-uri none + object-src none) / `off` kill-switch incident, skip CSP sur `/docs`/`/redoc`/`/openapi.json` quand preset != prod (Swagger UI inline JS), `_enforce_production_safety` étendu fail-fast si prod ET preset NOT IN {prod, off}. **3 settings** dans `app/config.py` (`security_headers_preset` Literal + `APP_VERSION`/`APP_COMMIT_SHA` env vars CI/CD). **51 tests pytest verts** (12 openapi schema + 17 health extended + 22 security headers). 0 régression. **Recommandation Ivan** : (1) poser `secrets.SECURITY_HEADERS_PRESET=prod` dans GitHub Settings AVANT déploiement L2 staging ; (2) modifier `release.sh` L1 pour poser `APP_VERSION=$tag` + `APP_COMMIT_SHA=$sha` dans le Dockerfile build args ; (3) HSTS preload = engagement long terme (soumission hstspreload.org), à faire post-stabilisation L2 prod. | ✅ | 2026-04-27 |
| O2 | DD-ready polishing (pré-levée optionnel) | **6 volets indépendants** — Volet A architecture (7 docs `overview/data-model/request-flow/ai-architecture/security-posture/observability/payments-readiness` Mermaid diagrams + spec FR exec summary EN), Volet B compliance (4 docs `rgpd/ai-act/security-checklist/dpa-template` mapping Articles + OWASP Top 10 + Article 28 sous-traitants), Volet C API contracts (`endpoints.md` table 60+ endpoints + `error-codes.md` 30+ codes + `versioning.md` policy + `openapi.json` exporté 6500 lignes), Volet D onboarding (`README.md` racine 250 lignes onboarding 5 min + `glossary.md` 50+ termes + 3 runbooks `incident-response/deployment-l2/db-restore` + 5 ADRs format Nygard `0001-fastapi-vs-django/0002-sqlalchemy-async/0003-redis-rate-limiting/0004-jwt-rs256-vs-hs256/0005-llm-router-mock-first`), Volet E scripts d'automation (`scripts/export_openapi.py` dump app.openapi → JSON pretty + `scripts/export_schema.sh` strict bash pg_dump --schema-only + Makefile targets `export-openapi`/`export-schema`/`export-dd` + workflow `.github/workflows/dd-exports-fresh.yml` push main vérifie git diff exit-code + open issue auto si stale), Volet F polishing docstrings (différé V2 — top 10 modules à documenter en passe ultérieure). 42 tests pytest verts (validation structure README+glossary+architecture+compliance+API+runbooks+ADRs format Nygard+migrations cross-check+scripts+workflow). 0 régression. Aucune nouvelle dep pip. Aucune migration DB. **Boucle Bloc O à 100% fermée** : O1+O2 ✅. **Recommandations Ivan pour DD** : kit complet `docs/` permet à un investisseur/consultant CTO/Tech Lead nouveau d'auditer NEXYA en 2-3h en autonomie. Pour mise à jour openapi.json + schema.sql, lancer `make export-dd` post-modification de routers/migrations + commit. Le workflow GHA `dd-exports-fresh.yml` fail le push main si stale. | ✅ | 2026-04-27 |

**Livrable bloc O :** backend prêt pour due diligence investisseurs, documentation propre, sécurité hardening niveau prod sérieux.

| P1.cleanup | Polish documentaire fin Période 1 | Drift §7 corrigé (2 lignes : Migrations Alembic 🔧→✅ + S3/MinIO ❌→✅), `nexya_backend/docker/docker-compose.prod.yml` minimal stub V1 (118 lignes : `nexya-api` GHCR + `nexya-worker` arq, sans pg/redis/minio/Caddy, header env vars complets), `nexya_backend/docs/pricing/PRICING_DECISIONS.md` gabarit décision Ivan (~430 lignes pour 16 settings TODO `provisoire` posés D4/E1/E2/E4/F1/J1/K2 — chaque setting documenté avec contexte coût 950k users + 3 options + ligne `TRANCHE IVAN`), 1 ligne lien dans `docs/architecture/overview.md`. 0 régression pytest. 0 nouveau test. 0 nouvelle dep. 0 migration. 0 setting tranché par l'IA (gabarit prêt pour Ivan). Coût $0. **Période 1 INFRA backend définitivement bouclée à 100 %.** | ✅ | 2026-04-29 |

---

## 🕒 Sessions différées V2 / Phase 17 — Hors scope V1 launch

> Sessions identifiées dans la discussion stratégique 2026-05-10 mais délibérément hors scope V1.
> **À ne PAS planifier avant que les conditions explicites soient remplies.**

| Référence | Session | Condition d'activation | Estimation |
|---|---|---|---|
| **NX.1** | **GrowthBook feature flags + A/B testing backend** | ≥ 2 000 users payants actifs (volume statistique pour A/B significatif) | ~12 h |
| **NX.2** | **Suggestions admin queue UI + workflow modération** | Volume `user_suggestions` ≥ 50 entries/semaine (sinon lecture email N1 suffit) | ~8 h |
| **NX.3** | **NPS admin dashboard temps réel + drill-down cohortes** | Bloc N5 livré ET ≥ 1 000 NPS responses collectés | ~6 h |

**Justification du report** : ces 3 sessions sont des optimisations de scale, pas des prérequis launch. Les activer trop tôt = code mort qui pèse sur la maintenance solo founder. Le déclencheur est un signal volumétrique objectif, pas une intuition produit.

---

# 📈 Récapitulatif global

| Bloc | Phase ROADMAP | Sessions | Heures | % du restant |
|---|---|---|---|---|
| A | Phase 1 résiduel (Auth) | 3 | 30 | 5.3 % |
| B | Phase 2 résiduel (Couche IA) | 3 | 30 | 5.3 % |
| C | Phase 5 (History/Projects/Library) | 3 | 30 | 5.3 % |
| D | Phase 6 (Memory + RAG) | 5 | 50 | 8.8 % |
| E | Phase 7 (Voice + Vision + Files + Watermarking) | 4 | 40 | 7.0 % |
| F | Phase 8 (Planner + Notifications + Deep linking) | 3 | 30 | 5.3 % |
| G | Phase 9 (7 Modes Experts RAG) | 7 | 70 | 12.3 % |
| H | Phase 10 (Gemma fine-tuning + MLOps) | 8 | 80 | 14.0 % |
| I | Phase 11 (Subscriptions + Paiements) | 4 | 40 | 7.0 % |
| J | Phase 12 (RGPD + AI Act backend) | 1 | 10 | 1.8 % |
| K | Phase 13 (Observabilité prod) | 2 | 20 | 3.5 % |
| L | Phase 14 (CI/CD + Staging + Prod + WAF) | 4 | 40 | 7.0 % |
| M | Phase 19 (Pentest + scaling + conformité) | 3 | 30 | 5.3 % |
| N | Tâches transversales backend | 5 | 48 | 8.3 % |
| O | Polish final & Documentation | 2 | 20 | 3.5 % |
| **TOTAL** | — | **58** | **578** | **100 %** |

---

# 🎯 Stratégies de progression — Choisir un chemin

## Chemin 1 — MVP backend prod-ready (le plus rapide)
> Pour lancer en prod sereinement sans Gemma local ni tous les experts.

**Blocs à faire :** A, B, C, D, E, F, I, J, K, L, N, O + 2 experts G1+G2
→ **43 sessions ≈ 7 semaines de pur dev intensif**
→ Reporter **Phase 9 (5 experts restants)**, **Phase 10 (Gemma)**, **Phase 19 (pentest)** en V2

## Chemin 2 — Backend complet sans pentest
> Tout, sauf l'audit externe (à faire avant 10k users payants).

**Blocs à faire :** A à O sauf M
→ **55 sessions ≈ 9 semaines**

## Chemin 3 — Backend exhaustif total
> La totale, prêt à scaler à 10k+ users sans recoder.

**Blocs à faire :** tous (A à O)
→ **58 sessions ≈ 9.5 semaines de pur dev intensif**
→ Avec apprentissage transversal en parallèle (~233 h) : compter **3 mois** réalistes

---

# 📚 Apprentissage transversal en parallèle (rappel)

> Pas du code, mais du temps obligatoire pour pouvoir faire les phases sans se planter.

| Sujet | Heures | Avant quel bloc |
|---|---|---|
| Python async + asyncio | 12 | A (déjà entamé) |
| FastAPI Tutorial + Advanced | 15 | A (idem) |
| SQLAlchemy 2.0 async | 10 | C (déjà entamé) |
| PostgreSQL avancé | 15 | C |
| pgvector + embeddings | 8 | D |
| Bases ML (fast.ai partie 1) | 30-40 | G/H |
| NLP / Transformers | 25 | H |
| Fine-tuning LoRA | 15 | H |
| MLOps | 20 | H |
| Docker / Linux | 20 | L |
| CI/CD GitHub Actions | 8 | L |
| OpenTelemetry + Grafana | 10 | K |
| Sécurité backend (OWASP Top 10) | 15 | M |
| Mobile Money / CinetPay | 6 | I |
| RevenueCat / IAP | 12 | I |
| RGPD + AI Act | 12 | J |
| **Total apprentissage backend pertinent** | **~233 h** | |

---

# 📝 Journal des sessions

> Format : date — bloc.session — durée réelle — résultat — fichiers impactés

| Date | Session | Durée | Résultat | Fichiers impactés |
|---|---|---|---|---|
| 2026-04-22 | A1 — Reset password + email transactionnel | ~10 h | ✅ Livrée — 18 tests verts, 92/92 suite complète, 0 régression | `app/core/email/*` (6 fichiers), `app/core/auth/password_reset.py`, `app/core/security/rate_limiter.py`, `app/features/auth/schemas.py`+`service.py`+`router.py`, `tests/test_password_reset.py`, 3 nouvelles exceptions, Settings Brevo |
| 2026-04-22 | A3 — Auth hardening (captcha + sanitizer + quotas + audit) | ~10 h | ✅ Livrée — 29 tests verts (4 couches défense `/register`), 121/121 suite complète, 0 régression | `app/core/security/sanitizer.py`, `app/core/security/captcha/*` (5 fichiers), `app/features/auth/device_quotas.py`+`auth_events.py`+`models.py`, `migrations/versions/003_auth_hardening.py`, `app/features/auth/schemas.py`+`service.py`+`router.py`, `app/core/security/rate_limiter.py`, `app/config.py`, 2 nouvelles exceptions, `tests/test_auth_hardening_a3.py` (500+ lignes), fix régression `tests/test_password_reset.py` |
| 2026-04-24 | F1 — Planner Scheduler (CRUD tâches + worker arq dispatch/execute/cleanup) | ~10 h | ✅ Livrée — 67 tests F1 verts en isolation (90 s), pattern `SELECT FOR UPDATE SKIP LOCKED` pour concurrence multi-workers, 4 schedule_type discriminés Pydantic v2 (once/interval/daily/weekly UTC), retry transient ProviderUnavailable 5 min × max 2 | `migrations/versions/014_scheduled_tasks.py`, `app/features/planner/*` (7 fichiers : models, schemas, scheduler helper, service 8 méthodes, router 8 endpoints), `workers/scheduler_tasks.py` (dispatch/execute/cleanup), `workers/worker.py` (3 functions + 2 crons), 2 exceptions (402 TASKS_QUOTA_EXCEEDED + 422 TASK_SCHEDULE_INVALID), 2 pricing TODO(Ivan) + 5 settings technique, 7 fichiers tests |
| 2026-05-04 | F0.5 — Extension Monthly + Yearly schedule types (mini-session déclenchée par F1 frontend pour préserver UI vs amputation) | ~30 min | ✅ livré | `MonthlyConfig` (`day` 1-31 + hour + minute) + `YearlyConfig` (`month` 1-12 + `day` 1-31 + hour + minute avec validator `_validate_month_day` rejetant 30 février et 31 avril mais acceptant 29 février → clamp à 28 sur année non bissextile) ajoutés à `app/features/planner/schemas.py`. Extension `ScheduleType` literal vers 6 valeurs (`once / interval_minutes / daily / weekly / monthly / yearly`). Helper `_make_monthly_candidate(year, month, day, hour, minute)` dans `scheduler.py` clamp `day` au dernier jour du mois via `calendar.monthrange()` (31 février → 28/29, 31 avril → 30, 31 juin → 30). `compute_next_run` étendu : 2 nouvelles branches (monthly = mois courant si futur sinon mois suivant ; yearly = année courante si futur sinon année suivante). **21 nouveaux tests** : 8 schemas (monthly valid + day=31 accepté + day=0/32 rejeté + yearly valid + 29 février accepté + 30 février/31 avril/13 mois rejetés) + 13 scheduler (monthly mois courant/suivant, clamp 31 avril → 30, clamp 31 février → 28 non bissextile / 29 bissextile, day invalide, décembre→janvier de l'année suivante, yearly année courante/suivante, 29 février → 28 non bissextile vs 29 bissextile, mois invalide). **86 tests verts en local** (`pytest tests/test_planner_*.py tests/test_scheduler_helpers.py tests/test_scheduler_execute_worker.py`), zéro régression. **0 migration DB** (`schedule_config` JSONB accepte n'importe quel dict), **0 nouvelle dep**, **0 nouvelle exception**. `service.py` et `router.py` inchangés (délégation à `compute_next_run` + `_schedule_to_dict` rend l'extension transparente). | `app/features/planner/schemas.py` (+59 lignes : MonthlyConfig + YearlyConfig + 6-arity discriminator union), `app/features/planner/scheduler.py` (+62 lignes : `import calendar` + 2 branches monthly/yearly + helper `_make_monthly_candidate`), `tests/test_planner_schemas.py` (+87 lignes : 8 nouveaux tests), `tests/test_scheduler_helpers.py` (+109 lignes : 13 nouveaux tests). |
| 2026-04-24 | G1 — Expert Langues RAG : INFRA livrée + scope corpus Tatoeba **abandonné** post-validation | ~12 h dont 4 h validation | ⚠️ **Infra ✅ / Scope produit abandonné** — 10 bugs trouvés et fixés (5 G1 + 1 héritage pré-G1 + 4 intégration Vertex AI), 10 000 paires Tatoeba ingérées via Vertex en 9'22, **blind test 13/30 (43 %) → ÉCHEC seuil 24/30**. Diagnostic : Gemini 2.5 Pro déjà excellent sur FR/EN/ES/PT, RAG redondant ; langues camerounaises → bloc H fine-tuning. Cleanup : `corpus_enabled=False` posé sur expert language, `DELETE FROM expert_corpus_chunks WHERE expert_slug='language'` à exécuter manuellement par Ivan. **Infra technique CONSERVÉE** (table, provider Vertex, pipeline ingestion) → réutilisable pour G2/G4/G6. Décision stratégique consolidée : 3 PÉRIODES groupées par compétence (P1 INFRA backend autonome, P2 IA-QUALITY dédiée avec affûtage prompts + RAG corpus + fine-tuning Gemma, P3 DÉPLOIEMENT prod). | `migrations/005_fts_search.py` (fix down_revision pré-G1), `app/ai/embeddings/{base,gemini_embeddings,runtime,mock_embeddings}.py` (switch Vertex + dim configurable), `app/ai/engine/query_engine.py` (TYPE_CHECKING circular fix), `app/features/experts/context_builder.py` (heuristique eng-fra), `app/features/experts/models.py` + `migrations/016_expert_corpus_chunks.py` (model rename), `app/ai/experts.py` (corpus_enabled=False expert language + commentaire), `scripts/import_expert_corpus_langues.py` (WindowsSelectorEventLoopPolicy + dry-run skip embed), `tests/eval_g1_langues/run_eval.py` (Vertex + EventLoopPolicy), `app/config.py` + `.env.example` (settings Vertex), `tests/test_gemini_embeddings_provider.py`, `tests/eval_g1_langues/report_2026-04-24.md`, mémoire `project_nexya_solo_pilot_plan.md` (3 périodes), `CLAUDE.md` §15 (post-mortem complet) |

---

# 🚦 Règles d'utilisation par Claude

1. **Au démarrage de chaque session**, Claude relit ce fichier + ROADMAP.md + CLAUDE.md, puis confirme avec un résumé court : « OK, Phase X bloc Y session Z, voici le plan, on y va ? »
2. **Pendant la session**, Claude trace l'avancement par cases cochées dans la table de la session courante.
3. **À la fin de la session**, Claude :
   - Coche `[x]` la ligne dans la table du bloc concerné, met la date dans « Fait le »
   - Ajoute une ligne dans le « Journal des sessions » ci-dessus
   - Met à jour le compteur global en haut du fichier (« Statut » + pourcentage)
   - Met à jour ROADMAP.md (cocher la tâche correspondante) et CLAUDE.md (journal §15)
4. **Si une session déborde** (réelle > 10 h), noter dans le journal et ajuster les estimations restantes en conséquence.
5. **Si un bloc se révèle plus simple ou plus complexe que prévu**, ajuster l'estimation des sessions du bloc et recalculer les pourcentages globaux.

| 2026-05-04 | F2 frontend Planner — finalisation mock-first (consommation endpoints existants) | aucune | ✅ aucun travail backend | F1 backend (8 endpoints /tasks) + F3 backend (/user/notification-preferences GET/PUT + /user/device-token POST/DELETE) déjà livrés — F2 frontend consomme contractuellement. Mock token UUID respecte ^(android|ios)$ regex + bornes [10..500] chars Pydantic. **0 modif schema/service/router/migration backend**. Session frontend a livré 9 nouveaux fichiers + 6 modifs côté Flutter avec pattern mock-first qui débloque H1 frontend (swap 1 ligne quand Ivan aura FCM_SERVER_KEY). | (aucun fichier backend touché) |
| 2026-05-19 | **Période 2 IA-Quality Phase A — Session A2 backend : Affûtage 11 Experts au niveau divin** | ~10 h | ✅ Livrée — 483 nouveaux tests A2 verts en 3.21 s + 694/694 tests cible régression verts (A1+A2+G1+G2+chat+experts) + 2 xfailed G1 langues attendus, 0 régression sur la suite cible. Mission Silicon Valley : transformer 11 system_prompts experts (5-15 lignes → 50-200 lignes ciselées) avec persona profonde + méthodologie + 2-4 templates + 2-3 few-shot examples calibrés + anti-patterns + 6 clauses transverses (multi-langue, memory-aware D3, format markdown, sources, progressive disclosure, continuité). Safety-critical hardening MAX : medicine bloc URGENCE + 5 symptômes + 117/118/119 ; legal OHADA + Code civil article exact. Cooking préserve G2 V8. Nouveau package `app/ai/expert_prompts/` (12 modules). Refactor experts.py imports propres + _with_guardrail centralisé. Réparation collatérale test_nexya_identity_preserved_on_all_experts cassé par A1. Multi-langue dynamique dès A2. Anti-jailbreak per expert. 0 migration, 0 dep, 0 endpoint. **Coût A2 : $0.** Boucle Période 2 Phase A à 2/3 fermée (A1 ✅ + A2 ✅, A3 fine-tuning Gemma reste). | `app/ai/expert_prompts/` (12 nouveaux modules : __init__ + _shared + general + computer + science + cooking + language + legal + medicine + finance + engineering + productivity + studio), `app/ai/experts.py` (refactor imports + _with_guardrail centralisé sur 9 experts spécialisés), `tests/test_expert_prompts_shared.py` (NEW ~30 tests), `tests/test_expert_prompts_per_expert.py` (NEW ~130 tests paramétrés), `tests/test_expert_prompts_specifics.py` (NEW ~70 tests métier), `tests/test_streaming_nexya_preamble_injection.py` (1 assertion marker actualisée), `CLAUDE.md` (§7 + §15), `docs/ROADMAP.md`, `docs/BACKEND_SESSIONS_PLAN.md` (cette entrée). |
| 2026-05-19 | **Période 2 IA-Quality Phase A — Session A1 backend : Fondations Identité + Ton + Routing NEXYA** | ~10 h | ✅ Livrée — 194 nouveaux tests A1 verts en 2.30 s + 0 régression ciblée sur 9/9 + 2 xfailed (G1 langues post-cleanup) sur les 95 tests critiques retouchés (`test_experts_registry`, `test_streaming_expert_corpus_concat`, `test_chat_stream_expert_corpus_injection`, `test_llm_router`, `test_chat_stream_persisted`). Mission Silicon Valley : transformer NEXYA AI de « IA générique enveloppée d'une marque » en assistant à identité, ton et intelligence de routing irréprochables. **4 nouveaux modules** sous `app/ai/` : `nexya_tone.py` (10 commandements FR+EN — tutoiement, no creuse opening « Bien sûr ! » bannies, anti-sycophancy, structure scannable, jargon décortiqué, Africa-first contextuel **non-exclusif** validation Ivan « pas mondial, europe surtout, sois sage », brièveté calibrée, exemples concrets, transparence sur limites, chaleur professionnelle mentor grand frère), `nexya_identity.py` (4 paliers progressifs Loth Ivan Ngassa Yimga / dev Flutter / Cameroun / Nexyalabs + sécurité brand technique anti-leak Gemini/GPT/Claude + description vendeuse 11 experts + 15 features magnifiques + anti-hallucination biographique strict + anti-exposition coordonnées perso + résistance prompt injection), `nexya_routing.py` (matrice cross-expert intelligent — cas Ivan critique : question informatique posée à expert Cuisine → redirect Computer **PAS Général** ; `detect_query_intent` keyword cascade FR+EN safety-critical first ; `get_routing_guidance` instruction LLM avec table d'aiguillage + 5 règles obligatoires ; immune prompt injection car matrice figée Python), `nexya_preamble.py` (assembleur `build_nexya_preamble(expert_id, locale='fr')` avec ordre figé `tone → routing → identity`, cap 12000 chars défaut ~3000 tokens overhead acceptable, fail-safe absolue kill-switch + exception → None pattern aligné D3 memory + G1 corpus, idempotence stricte byte-à-byte). **Wiring `streaming.py`** : 2 modifs chirurgicales — import `build_nexya_preamble` + `_stream_link` ligne 578 nouvel ordre `parts = [nexya_preamble, ctx.memory_context, ctx.expert_corpus_context, rag_block, config.system_prompt]` — preamble EN TÊTE de tout. **Cleanup `experts.py`** : `_NEXYA_IDENTITY` legacy (lignes 97-110) transformé en `""` avec commentaire explicatif (rétrocompat 100 % des 11 concatenations sans toucher aux 11 `_X_PROMPT` qui deviennent purement métier). Préserve intégralement `_DOMAIN_GUARDRAIL_TEMPLATE` G2 + disclaimers safety-critical medicine/legal + `corpus_enabled=True` cooking. **3 nouveaux settings** : `nexya_preamble_enabled: bool = True` + `nexya_preamble_max_chars: int = 12000` + `nexya_preamble_default_locale: Literal['fr','en'] = 'fr'`. **Aucune migration DB, aucune nouvelle dep pip, aucun nouvel endpoint HTTP.** 6 fichiers tests (`test_nexya_tone.py` 40 tests + `test_nexya_identity.py` 69 tests + `test_nexya_routing.py` 43 tests + `test_nexya_preamble.py` 35 tests + `test_streaming_nexya_preamble_injection.py` 7 tests + fixes collatéraux 2 tests). **Décisions architecturales A1→A8 documentées** (CLAUDE.md §15 entrée 2026-05-19). **Coût A1 : $0** (pure infra Python + tests mock-first, zéro appel LLM). Checklist Ivan post-merge test manuel terrain : 5 questions via `/chat/stream` Vertex AI sur device réel (« Qui es-tu ? » palier 1, « Qui t'a créé en détail ? » palier 2/3 sans hallucination, « Tu utilises Gemini ? » esquive élégante, mode Cuisine + question Python → routing Informatique **PAS Général**, recette ndolé via corpus G2). **Boucle Période 2 IA-Quality Phase A ouverte** (A1 ✅, A2 affûtage prompts experts individuels + A3 fine-tuning Gemma langues vernaculaires camerounaises restent à coder). | `app/ai/nexya_tone.py` (NEW ~200 lignes), `app/ai/nexya_identity.py` (NEW ~400 lignes), `app/ai/nexya_routing.py` (NEW ~330 lignes), `app/ai/nexya_preamble.py` (NEW ~170 lignes), `app/ai/streaming.py` (wiring +2 lignes), `app/ai/experts.py` (cleanup `_NEXYA_IDENTITY=""`), `app/config.py` (+3 settings), `.env.example` (+3 vars), `tests/test_nexya_tone.py` (NEW 40 tests), `tests/test_nexya_identity.py` (NEW 69 tests), `tests/test_nexya_routing.py` (NEW 43 tests), `tests/test_nexya_preamble.py` (NEW 35 tests), `tests/test_streaming_nexya_preamble_injection.py` (NEW 7 tests), `tests/test_streaming_expert_corpus_concat.py` (fixture autouse disable preamble), `tests/test_chat_stream_expert_corpus_injection.py` (marker fix), `CLAUDE.md` (§7 ligne A1 + §15 entrée détaillée 2026-05-19), `docs/ROADMAP.md` (entrée journal), `docs/BACKEND_SESSIONS_PLAN.md` (cette entrée). |
