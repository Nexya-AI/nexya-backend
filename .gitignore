# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environment
venv/
.venv/

# Environment variables
.env

# JWT RSA keys — NE JAMAIS COMMITTER
*.pem
private.pem
public.pem

# PgBouncer userlist — contient les hash SCRAM des passwords Postgres
# Le `.example` est commité comme template, le vrai userlist.txt non
docker/pgbouncer/userlist.txt

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Models (stored in models_local/ at repo root)
*.gguf

# docs/ : ignoré par défaut MAIS on exempte les dossiers DD-ready
# livrés en O2 (architecture/compliance/api/runbooks/adr/glossary)
# + pricing (interne, repo privé).
docs/*
!docs/architecture/
!docs/compliance/
!docs/api/
!docs/runbooks/
!docs/adr/
!docs/audit/
!docs/glossary.md
!docs/ROADMAP.md
!docs/BACKEND_SESSIONS_PLAN.md
!docs/pricing/

# Évals IA — rapports transients (la baseline est committée, pas les rapports)
tests/evals/reports/
